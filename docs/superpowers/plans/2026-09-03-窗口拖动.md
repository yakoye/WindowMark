# 窗口拖动 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 按住可配置的修饰键 + 左键拖动移动窗口、右键拖动缩放窗口，完全自己实现，不引入 AltSnap 的任何代码。

**Architecture:** 纯计算（修饰键解析、九宫格判定、矩形计算）进 `windowmark_core` 并有单测；`WH_MOUSE_LL` 钩子和窗口操作留 `WinDragBackend`。键盘钩子只在配置勾了 Win 键时安装。

**Tech Stack:** C++20 / Win32 / CMake / MSVC（`/W4 /WX`，警告即错误）

## Global Constraints

- 设计依据：`docs/superpowers/specs/2026-09-03-窗口拖动-design.md`
- **不读、不抄 AltSnap 的源码**。它是 GPL-3.0，WindowMark 是 MIT（v0.4.7 起）
- 左键拖 = 移动，右键拖 = 缩放；不做边缘吸附、主副动作调换、组合键激活
- 默认 `drag.enabled=false`、`drag.modifiers=RAlt`
- 九宫格 **1:1:1 等分**，正中间等同移动
- 缩放下限 `SM_CXMINTRACK` / `SM_CYMINTRACK`
- 拖动中途松开修饰键**以鼠标键为准**，继续到鼠标键松开
- 必须吞掉：手势用掉的**右键抬起**（否则弹上下文菜单）、勾了 Win 键时的 **Win 抬起**（否则弹开始菜单）
- 「暂停所有」**包含**窗口拖动（与剪贴板守护不同）
- 编译零警告，转义检查通过
- **Bash 工具里 python 跑不起来（返回 49），所有 python 一律用 PowerShell 工具跑**
- **不要用 `python -c` 传中文**；脚本用 Write 工具写成文件再执行
- Git Bash 里 `taskkill /IM` 会被 MSYS 转成路径，用 `powershell -Command "Stop-Process -Name X -Force"`
- 构建：`cmake --build build --config Release --parallel`
- 跑单测：`build\Release\windowmark_core_tests.exe`
- 装机：`powershell -NoProfile -ExecutionPolicy Bypass -File .\reinstall.ps1`

---

### Task 1: 修饰键解析（core）

**Files:**
- Modify: `include/windowmark/core/Hotkey.h`、`src/core/Hotkey.cpp`（暴露 `ParseKeyName`）
- Create: `include/windowmark/core/DragModifiers.h`、`src/core/DragModifiers.cpp`
- Modify: `CMakeLists.txt`、`tests/core_tests.cpp`

**Interfaces:**
- Produces:
  - `windowmark::ParseKeyName(std::string_view) -> unsigned`（Hotkey.h 新增）
  - `struct windowmark::DragModifiers { std::vector<unsigned> keys; bool Empty() const; bool Contains(unsigned vk) const; }`
  - `windowmark::ParseDragModifiers(std::string_view) -> DragModifiers`
  - `windowmark::FormatDragModifiers(const DragModifiers&) -> std::string`
  - `struct windowmark::PresetModifier { unsigned vk; const char* name; const wchar_t* label; }`
  - `windowmark::PresetModifiers() -> const std::vector<PresetModifier>&`

- [ ] **Step 1: 先写会失败的测试**

`tests/core_tests.cpp` include 区加（按字母序在 `ConfigLocation.h` 之后）：

```cpp
#include "windowmark/core/DragModifiers.h"
```

匿名 namespace 结尾前加入：

```cpp
void TestDragModifiers() {
    // 六个预设各自能解析，且往返稳定
    for (const auto& preset : PresetModifiers()) {
        const DragModifiers one = ParseDragModifiers(preset.name);
        CHECK(one.keys.size() == 1);
        CHECK(one.Contains(preset.vk));
        CHECK(FormatDragModifiers(one) == preset.name);
    }

    // 左右必须分得开——整个功能的前提
    const DragModifiers rightAlt = ParseDragModifiers("RAlt");
    CHECK(rightAlt.Contains(0xA5));    // VK_RMENU
    CHECK(!rightAlt.Contains(0xA4));   // VK_LMENU

    // 多个键：任一按下即触发，所以是集合不是组合
    const DragModifiers two = ParseDragModifiers("RAlt|LWin");
    CHECK(two.keys.size() == 2);
    CHECK(two.Contains(0xA5));
    CHECK(two.Contains(0x5B));         // VK_LWIN

    // 大小写、空格、顺序都不影响
    CHECK(ParseDragModifiers("ralt | lwin") == two);
    CHECK(ParseDragModifiers("LWin|RAlt") == two);
    // 规范形式按固定顺序输出，与输入顺序无关
    CHECK(FormatDragModifiers(ParseDragModifiers("LWin|RAlt")) == "RAlt|LWin");

    // 配置文件是手写的：一个拼错的名字只跳过它自己，不该让整个功能失效
    const DragModifiers partial = ParseDragModifiers("RAlt|Nonsense|LCtrl");
    CHECK(partial.keys.size() == 2);
    CHECK(partial.Contains(0xA5));
    CHECK(partial.Contains(0xA2));     // VK_LCONTROL

    // 空 = 不启用任何触发键
    CHECK(ParseDragModifiers("").Empty());
    CHECK(ParseDragModifiers("   ").Empty());
    CHECK(FormatDragModifiers(DragModifiers{}) == "");

    // 重复的名字不该产生重复的键
    CHECK(ParseDragModifiers("RAlt|RAlt").keys.size() == 1);

    // 预设之外的任意键：复用 Hotkey 的键名表，spec 要求配置文件能补任意按键
    const DragModifiers f13 = ParseDragModifiers("F13");
    CHECK(f13.keys.size() == 1);
    CHECK(f13.Contains(0x7C));         // VK_F13

    // ParseKeyName 本身
    CHECK(ParseKeyName("F5") == 0x74);
    CHECK(ParseKeyName("space") == 0x20);
    CHECK(ParseKeyName("A") == 'A');
    CHECK(ParseKeyName("Nonsense") == 0);
}
```

`main()` 里 `TestConfigLocationPriority();` 之后加：

```cpp
    TestDragModifiers();
```

- [ ] **Step 2: 跑一次确认编译不过**

```bash
cmake --build build --config Release --parallel
```

预期：找不到 `windowmark/core/DragModifiers.h`。

- [ ] **Step 3: 暴露 ParseKeyName**

`include/windowmark/core/Hotkey.h`，在 `ParseHotkey` 声明之前加：

```cpp
// 单个按键名 → Win32 虚拟键码。"F5"、"Space"、"A"、"1" 都认，不认识返回 0。
//
// 暴露出来是给 DragModifiers 复用同一张键名表的：两份表必然漂移，而「用户在配置文件里
// 写的键名叫什么」正是最不该在两处各有一套答案的东西。
[[nodiscard]] unsigned ParseKeyName(std::string_view name);
```

`src/core/Hotkey.cpp` 里，把 `ParseHotkey` 内部查表那段提取成这个公开函数——它现在应当
处理三类：`kNamedKeys` 表中的名字、`F1`–`F24`、单个字母或数字。`ParseHotkey` 改为调用它，
行为不变（Step 5 会用既有的 33 条快捷键用例验证没有回归）。

- [ ] **Step 4: 写 DragModifiers**

`include/windowmark/core/DragModifiers.h`：

```cpp
#pragma once

#include <string>
#include <string_view>
#include <vector>

namespace windowmark {

// 触发窗口拖动的修饰键集合。**任一按下即触发**，不是组合键——这正是它不能复用 Hotkey
// 的原因：Hotkey 表达「同时按住 Ctrl 和 Alt 和 T」，这里表达「右 Alt 或者左 Win」。
//
// keys 存 Win32 虚拟键码。平台值放在 core 是刻意的，理由同 Hotkey.h：只有一个后端，
// 再定义一套平行枚举等于一张两边都没有内容的翻译表。
struct DragModifiers {
    std::vector<unsigned> keys;

    [[nodiscard]] bool Empty() const { return keys.empty(); }
    [[nodiscard]] bool Contains(unsigned vk) const;

    friend bool operator==(const DragModifiers& a, const DragModifiers& b) {
        return a.keys == b.keys;
    }
};

// 解析 "RAlt|LWin"。大小写不敏感，容忍分隔符周围的空格。
//
// 不认识的名字**跳过它自己**而不是拒绝整条：配置文件是手写的，一个拼错不该让整个功能
// 静默失效——那种失败最难排查，因为看起来一切正常。
[[nodiscard]] DragModifiers ParseDragModifiers(std::string_view text);

// 规范形式，按 LAlt RAlt LWin RWin LCtrl RCtrl 的固定顺序，其余键附在后面。
// 与输入顺序无关，这样往返一次就稳定下来，配置文件不会因为重排而产生无谓的 diff。
[[nodiscard]] std::string FormatDragModifiers(const DragModifiers& mods);

// 设置界面的六个复选框。name 是配置文件里的写法，label 是界面上显示的中文。
struct PresetModifier {
    unsigned vk;
    const char* name;
    const wchar_t* label;
};

[[nodiscard]] const std::vector<PresetModifier>& PresetModifiers();

} // namespace windowmark
```

`src/core/DragModifiers.cpp`：

```cpp
#include "windowmark/core/DragModifiers.h"

#include "windowmark/core/Hotkey.h"

#include <algorithm>
#include <cctype>

namespace windowmark {
namespace {

// Win32 虚拟键码。写在这里而不是 include <windows.h>，与 Hotkey.cpp 同一个理由：
// core 不该为了六个常量拖进整个 windows.h。
constexpr unsigned kVkLShift = 0xA0;
constexpr unsigned kVkRShift = 0xA1;
constexpr unsigned kVkLControl = 0xA2;
constexpr unsigned kVkRControl = 0xA3;
constexpr unsigned kVkLMenu = 0xA4;
constexpr unsigned kVkRMenu = 0xA5;
constexpr unsigned kVkLWin = 0x5B;
constexpr unsigned kVkRWin = 0x5C;

// 顺序即 FormatDragModifiers 的输出顺序，也是设置界面里复选框的排列顺序。
const std::vector<PresetModifier>& Presets() {
    static const std::vector<PresetModifier> presets{
        {kVkLMenu, "LAlt", L"左 Alt 键"},
        {kVkRMenu, "RAlt", L"右 Alt 键"},
        {kVkLWin, "LWin", L"左 Win 键"},
        {kVkRWin, "RWin", L"右 Win 键"},
        {kVkLControl, "LCtrl", L"左 Ctrl 键"},
        {kVkRControl, "RCtrl", L"右 Ctrl 键"},
    };
    return presets;
}

// 预设之外还认的名字：左右 Shift。Shift 不在六个复选框里（它常被应用自己用），
// 但配置文件写了就认。
constexpr std::pair<const char*, unsigned> kExtraNames[]{
    {"LSHIFT", kVkLShift},
    {"RSHIFT", kVkRShift},
};

std::string Upper(std::string_view text) {
    std::string out;
    out.reserve(text.size());
    for (const char c : text) {
        out.push_back(static_cast<char>(std::toupper(static_cast<unsigned char>(c))));
    }
    return out;
}

void Trim(std::string& value) {
    const auto notSpace = [](unsigned char c) { return !std::isspace(c); };
    value.erase(value.begin(), std::find_if(value.begin(), value.end(), notSpace));
    value.erase(std::find_if(value.rbegin(), value.rend(), notSpace).base(), value.end());
}

[[nodiscard]] unsigned LookupOne(std::string_view token) {
    std::string name(token);
    Trim(name);
    if (name.empty()) return 0;
    const std::string upper = Upper(name);

    for (const auto& preset : Presets()) {
        if (upper == Upper(preset.name)) return preset.vk;
    }
    for (const auto& [text, vk] : kExtraNames) {
        if (upper == text) return vk;
    }
    // 落到 Hotkey 的键名表：F1-F24、Space、字母数字等。spec 要求配置文件能补任意按键。
    return ParseKeyName(name);
}

} // namespace

bool DragModifiers::Contains(unsigned vk) const {
    return std::find(keys.begin(), keys.end(), vk) != keys.end();
}

DragModifiers ParseDragModifiers(std::string_view text) {
    DragModifiers out;
    std::size_t start = 0;
    while (start <= text.size()) {
        const std::size_t bar = text.find('|', start);
        const std::size_t end = bar == std::string_view::npos ? text.size() : bar;
        const unsigned vk = LookupOne(text.substr(start, end - start));
        if (vk != 0 && !out.Contains(vk)) out.keys.push_back(vk);
        if (bar == std::string_view::npos) break;
        start = bar + 1;
    }
    return out;
}

std::string FormatDragModifiers(const DragModifiers& mods) {
    std::string out;
    const auto append = [&out](const char* name) {
        if (!out.empty()) out.push_back('|');
        out += name;
    };

    // 先按预设的固定顺序输出，剩下的再附在后面——这样输出与输入顺序无关。
    for (const auto& preset : Presets()) {
        if (mods.Contains(preset.vk)) append(preset.name);
    }
    for (const unsigned vk : mods.keys) {
        const bool isPreset = std::any_of(Presets().begin(), Presets().end(),
                                          [vk](const PresetModifier& p) { return p.vk == vk; });
        if (isPreset) continue;
        bool named = false;
        for (const auto& [text, extraVk] : kExtraNames) {
            if (extraVk == vk) {
                append(text);
                named = true;
                break;
            }
        }
        if (!named) append(FormatKeyName(vk).c_str());
    }
    return out;
}

const std::vector<PresetModifier>& PresetModifiers() { return Presets(); }

} // namespace windowmark
```

**注意** `FormatDragModifiers` 用到了 `FormatKeyName(unsigned) -> std::string`——`Hotkey.cpp`
里已有反向查表的逻辑（`FormatHotkey` 用它拼键名），一并提取成公开函数，声明加在
`Hotkey.h` 的 `ParseKeyName` 旁边：

```cpp
// 虚拟键码 → 规范键名，ParseKeyName 的反函数。无法命名的键返回 ""。
[[nodiscard]] std::string FormatKeyName(unsigned vk);
```

- [ ] **Step 5: 加进构建并跑测试**

`CMakeLists.txt` 的 core 源文件列表里，在 `src/core/Coordinator.cpp` 之后按字母序插入：

```cmake
    src/core/DragModifiers.cpp
```

```bash
cmake --build build --config Release --parallel
build\Release\windowmark_core_tests.exe
```

预期：零警告，`WindowMark core tests passed.`。既有的 `TestHotkeyParsing`（33 条断言）
必须一并通过——那是 `ParseKeyName` 提取没有改坏 `ParseHotkey` 的证明。

- [ ] **Step 6: 变异测试，确认「左右可分」这条真被钉住**

把 `Presets()` 里 `RAlt` 的 `kVkRMenu` 临时改成 `kVkLMenu`，重新构建并跑测试，确认
`TestDragModifiers` 失败。然后改回来再跑一次确认通过。

左右不分等于整个功能失去意义（按左 Alt 也会触发，和普通 Alt+拖动冲突），而这种错误肉眼
极难发现——所以要确认测试真的拦得住。

- [ ] **Step 7: 提交**

```bash
git add include/windowmark/core/DragModifiers.h src/core/DragModifiers.cpp include/windowmark/core/Hotkey.h src/core/Hotkey.cpp CMakeLists.txt tests/core_tests.cpp
git commit -m "窗口拖动：修饰键解析，左右键分得开"
```

---

### Task 2: 九宫格与矩形计算（core）

**Files:**
- Create: `include/windowmark/core/DragGeometry.h`、`src/core/DragGeometry.cpp`
- Modify: `CMakeLists.txt`、`tests/core_tests.cpp`

**Interfaces:**
- Consumes: `windowmark::Rect`（`Types.h`）
- Produces:
  - `struct windowmark::DragEdges { bool left, top, right, bottom; bool IsMove() const; bool None() const; }`
  - `windowmark::HitZone(const Rect& frame, int x, int y) -> DragEdges`
  - `windowmark::ApplyDrag(const Rect& start, const DragEdges&, int dx, int dy, int minWidth, int minHeight) -> Rect`

- [ ] **Step 1: 先写会失败的测试**

`tests/core_tests.cpp` include 区加：

```cpp
#include "windowmark/core/DragGeometry.h"
```

匿名 namespace 结尾前加入：

```cpp
void TestDragGeometry() {
    // 300x300 的窗口，九宫格每格正好 100
    const Rect frame{0, 0, 300, 300};

    // 四角各拉两条边
    {
        const DragEdges tl = HitZone(frame, 50, 50);
        CHECK(tl.left && tl.top && !tl.right && !tl.bottom);
        const DragEdges br = HitZone(frame, 250, 250);
        CHECK(!br.left && !br.top && br.right && br.bottom);
        const DragEdges tr = HitZone(frame, 250, 50);
        CHECK(!tr.left && tr.top && tr.right && !tr.bottom);
        const DragEdges bl = HitZone(frame, 50, 250);
        CHECK(bl.left && !bl.top && !bl.right && bl.bottom);
    }

    // 四边各拉一条
    {
        const DragEdges top = HitZone(frame, 150, 50);
        CHECK(!top.left && top.top && !top.right && !top.bottom);
        const DragEdges right = HitZone(frame, 250, 150);
        CHECK(!right.left && !right.top && right.right && !right.bottom);
    }

    // 正中间 = 移动（四条边一起动），否则中间按下无事可做
    {
        const DragEdges middle = HitZone(frame, 150, 150);
        CHECK(middle.IsMove());
    }

    // 边界值：正好落在格线上。100 属于中间格（左闭右开），这条必须钉死，
    // 否则 off-by-one 会让某一列的行为随窗口尺寸漂移。
    {
        CHECK(!HitZone(frame, 100, 150).left);
        CHECK(HitZone(frame, 99, 150).left);
        CHECK(HitZone(frame, 200, 150).right);
        CHECK(!HitZone(frame, 199, 150).right);
    }

    // 极窄窗口：格子退化也不能崩，也不能算出既 left 又 right
    {
        const Rect thin{0, 0, 2, 2};
        const DragEdges e = HitZone(thin, 1, 1);
        CHECK(!(e.left && e.right && !e.IsMove()));
    }

    // 窗口不在原点时，命中判断按相对位置
    {
        const Rect offset{1000, 500, 1300, 800};
        CHECK(HitZone(offset, 1050, 550).left);
        CHECK(HitZone(offset, 1250, 750).bottom);
    }

    // 移动：四条边同幅度平移，尺寸不变
    {
        const DragEdges move{true, true, true, true};
        const Rect moved = ApplyDrag(frame, move, 40, -20, 100, 100);
        CHECK(moved.left == 40);
        CHECK(moved.top == -20);
        CHECK(moved.width() == 300);
        CHECK(moved.height() == 300);
    }

    // 缩放右下角
    {
        const DragEdges br{false, false, true, true};
        const Rect sized = ApplyDrag(frame, br, 50, 30, 100, 100);
        CHECK(sized.left == 0);
        CHECK(sized.top == 0);
        CHECK(sized.right == 350);
        CHECK(sized.bottom == 330);
    }

    // 缩放左上角：动的是左上，右下不动
    {
        const DragEdges tl{true, true, false, false};
        const Rect sized = ApplyDrag(frame, tl, 20, 10, 100, 100);
        CHECK(sized.left == 20);
        CHECK(sized.top == 10);
        CHECK(sized.right == 300);
        CHECK(sized.bottom == 300);
    }

    // 最小尺寸：继续拖也不会缩过头，更不会反转
    {
        const DragEdges br{false, false, true, true};
        const Rect clamped = ApplyDrag(frame, br, -400, -400, 100, 100);
        CHECK(clamped.width() == 100);
        CHECK(clamped.height() == 100);
        CHECK(clamped.left == 0);
        CHECK(clamped.top == 0);
    }

    // 从左上角缩到下限时，固定的应当是右下角
    {
        const DragEdges tl{true, true, false, false};
        const Rect clamped = ApplyDrag(frame, tl, 400, 400, 100, 100);
        CHECK(clamped.width() == 100);
        CHECK(clamped.height() == 100);
        CHECK(clamped.right == 300);
        CHECK(clamped.bottom == 300);
    }

    // 移动不受最小尺寸影响
    {
        const DragEdges move{true, true, true, true};
        const Rect moved = ApplyDrag(frame, move, -5000, -5000, 100, 100);
        CHECK(moved.width() == 300);
        CHECK(moved.height() == 300);
    }
}
```

`main()` 里 `TestDragModifiers();` 之后加：

```cpp
    TestDragGeometry();
```

- [ ] **Step 2: 跑一次确认编译不过**

```bash
cmake --build build --config Release --parallel
```

预期：找不到 `windowmark/core/DragGeometry.h`。

- [ ] **Step 3: 写头文件**

`include/windowmark/core/DragGeometry.h`：

```cpp
#pragma once

#include "windowmark/core/Types.h"

namespace windowmark {

// 拖动改变窗口矩形的哪几条边。四条全为真 = 移动（整体平移）。
struct DragEdges {
    bool left{};
    bool top{};
    bool right{};
    bool bottom{};

    [[nodiscard]] bool IsMove() const { return left && top && right && bottom; }
    [[nodiscard]] bool None() const { return !left && !top && !right && !bottom; }
};

// 鼠标按下的位置落在窗口的哪一格。九宫格 1:1:1 等分，正中间返回「移动」——否则在窗口
// 中间按下右键将无事可做。
//
// 格线归属：左闭右开。窗口宽 300 时，x=100 属于中间列而不是左列。这个约定必须稳定，
// off-by-one 会让某一列的行为随窗口尺寸漂移，而那种 bug 靠肉眼几乎发现不了。
[[nodiscard]] DragEdges HitZone(const Rect& frame, int x, int y);

// 起始矩形 + 光标位移 → 新矩形。
//
// 缩放受 minWidth / minHeight 限制：到达下限后继续拖不再改变尺寸，被拖的那条边停住，
// 对面那条边始终不动。没有这个夹取，窗口会被拖成一条线甚至左右反转。
// 移动不受限制——把窗口拖出屏幕是用户的自由，Windows 自己也允许。
[[nodiscard]] Rect ApplyDrag(const Rect& start, const DragEdges& edges, int dx, int dy,
                             int minWidth, int minHeight);

} // namespace windowmark
```

- [ ] **Step 4: 写实现**

`src/core/DragGeometry.cpp`：

```cpp
#include "windowmark/core/DragGeometry.h"

#include <algorithm>

namespace windowmark {
namespace {

// 把一维坐标归到 0/1/2 三格之一。左闭右开，超出范围夹到两端。
[[nodiscard]] int Band(int value, int start, int extent) {
    if (extent <= 0) return 1;
    const int offset = value - start;
    if (offset < extent / 3) return 0;
    if (offset < extent - extent / 3) return 1;
    return 2;
}

} // namespace

DragEdges HitZone(const Rect& frame, int x, int y) {
    const int col = Band(x, frame.left, frame.width());
    const int row = Band(y, frame.top, frame.height());

    if (col == 1 && row == 1) return DragEdges{true, true, true, true};

    DragEdges edges;
    edges.left = col == 0;
    edges.right = col == 2;
    edges.top = row == 0;
    edges.bottom = row == 2;
    return edges;
}

Rect ApplyDrag(const Rect& start, const DragEdges& edges, int dx, int dy, int minWidth,
               int minHeight) {
    if (edges.IsMove()) {
        return Rect{start.left + dx, start.top + dy, start.right + dx, start.bottom + dy};
    }

    Rect out = start;
    if (edges.left) out.left = start.left + dx;
    if (edges.right) out.right = start.right + dx;
    if (edges.top) out.top = start.top + dy;
    if (edges.bottom) out.bottom = start.bottom + dy;

    // 夹到下限时，动的是被拖的那条边，对面那条始终保持不动。
    const int minW = std::max(1, minWidth);
    const int minH = std::max(1, minHeight);
    if (out.right - out.left < minW) {
        if (edges.left) {
            out.left = out.right - minW;
        } else {
            out.right = out.left + minW;
        }
    }
    if (out.bottom - out.top < minH) {
        if (edges.top) {
            out.top = out.bottom - minH;
        } else {
            out.bottom = out.top + minH;
        }
    }
    return out;
}

} // namespace windowmark
```

- [ ] **Step 5: 加进构建并跑测试**

`CMakeLists.txt` 的 core 源文件列表里，在 `src/core/DragModifiers.cpp` 之前插入
（字母序 DragGeometry < DragModifiers）：

```cmake
    src/core/DragGeometry.cpp
```

```bash
cmake --build build --config Release --parallel
build\Release\windowmark_core_tests.exe
```

预期：零警告，全部通过。

- [ ] **Step 6: 变异测试，确认最小尺寸夹取真被钉住**

把 `ApplyDrag` 里那两段夹取整个删掉，重新构建跑测试，确认「最小尺寸」和「从左上角缩到
下限」两组用例失败。然后恢复再跑一次确认通过。

没有夹取时窗口会被拖成一条线甚至左右反转，而这只在用力拖过头时才出现——正是手工测试
容易漏掉的场景。

- [ ] **Step 7: 提交**

```bash
git add include/windowmark/core/DragGeometry.h src/core/DragGeometry.cpp CMakeLists.txt tests/core_tests.cpp
git commit -m "窗口拖动：九宫格判定与矩形计算，含最小尺寸夹取"
```

---

### Task 3: 配置项（core）

**Files:**
- Modify: `include/windowmark/core/Settings.h`、`src/core/Settings.cpp`
- Modify: `tests/core_tests.cpp`

**Interfaces:**
- Consumes: Task 1 的 `ParseDragModifiers` / `FormatDragModifiers`
- Produces: `windowmark::Settings::drag`，类型 `DragSettings`：

```cpp
struct DragSettings {
    std::vector<std::string> excludedAppKeys;
    std::string modifiers{"RAlt"};
    bool enabled{false};
};
```

- [ ] **Step 1: 先写会失败的测试**

`tests/core_tests.cpp` 匿名 namespace 结尾前加入：

```cpp
void TestDragSettingsRoundTrip() {
    // 默认值：功能默认关闭，触发键默认只有右 Alt
    Settings fresh;
    CHECK(!fresh.drag.enabled);
    CHECK(fresh.drag.modifiers == "RAlt");
    CHECK(fresh.drag.excludedAppKeys.empty());

    // 存一次读回来，三个字段都不能变——这是发现读写不对称最有效的手段
    const auto path = std::filesystem::temp_directory_path() / "windowmark-drag-test.conf";
    Settings written;
    written.drag.enabled = true;
    written.drag.modifiers = "RAlt|LWin";
    written.drag.excludedAppKeys = {"c:/apps/photoshop.exe", "c:/apps/gimp.exe"};
    CHECK(Settings::Save(path, written));

    const Settings read = Settings::LoadOrCreate(path);
    CHECK(read.drag.enabled);
    CHECK(read.drag.modifiers == "RAlt|LWin");
    CHECK(read.drag.excludedAppKeys.size() == 2);
    CHECK(read.drag.excludedAppKeys[0] == "c:/apps/photoshop.exe");
    CHECK(read.drag.excludedAppKeys[1] == "c:/apps/gimp.exe");

    // 存进去的修饰键必须能被解析回同一组键
    const DragModifiers parsed = ParseDragModifiers(read.drag.modifiers);
    CHECK(parsed.keys.size() == 2);
    CHECK(parsed.Contains(0xA5));   // VK_RMENU
    CHECK(parsed.Contains(0x5B));   // VK_LWIN

    std::error_code ec;
    std::filesystem::remove(path, ec);
}
```

`main()` 里 `TestDragGeometry();` 之后加：

```cpp
    TestDragSettingsRoundTrip();
```

- [ ] **Step 2: 跑一次确认失败**

```bash
cmake --build build --config Release --parallel
```

预期：`Settings` 没有 `drag` 成员，编译失败。

- [ ] **Step 3: 加 DragSettings**

`include/windowmark/core/Settings.h`，在 `BorderSettings` 之后加：

```cpp
// 按住修饰键拖动窗口。默认关闭：它接管全局鼠标事件，不该在用户没要求时就生效。
struct DragSettings {
    // 不参与拖动的应用，键与 border.excludedAppKeys 同样是规范化的可执行文件路径。
    // 独立成一项而不是共用边框那份：有些程序自己就用 Alt+拖动（Photoshop 等），
    // 不想要拖动和不想要边框是两件事。
    std::vector<std::string> excludedAppKeys;

    // 触发键，"RAlt" / "RAlt|LWin"。任一按下即触发，不是组合键。
    // 存字符串而不是解析后的键码：配置文件是手写的，原样保留用户写的东西——包括六个
    // 预设之外的键名——比存一份解析结果更不容易丢信息。
    std::string modifiers{"RAlt"};

    bool enabled{false};
};
```

`Settings` 结构里加成员（放在 `border` 之后）：

```cpp
    DragSettings drag;
```

- [ ] **Step 4: 读写**

`src/core/Settings.cpp` 的解析部分，在 `border.excluded_apps` 那段之后加：

```cpp
    if (const auto it = values.find("drag.enabled"); it != values.end()) {
        settings.drag.enabled = ParseBool(it->second, settings.drag.enabled);
    }
    if (const auto it = values.find("drag.modifiers"); it != values.end()) {
        settings.drag.modifiers = it->second;
    }
    if (const auto it = values.find("drag.excluded_apps"); it != values.end()) {
        settings.drag.excludedAppKeys = DecodeList(it->second);
    }
```

**注意**：`border.excluded_apps` 那段用的解码函数叫什么，就照抄什么名字（`EncodeList` 的
反函数）。

序列化部分，在 `border.excluded_apps=` 那行之后加：

```cpp
    output << "\n";
    output << "# 按住修饰键拖动窗口：左键拖=移动，右键拖=缩放。默认关闭。\n";
    output << "# modifiers 里的键**任一按下即触发**，不是组合键。多个用 | 分隔。\n";
    output << "# 六个预设：LAlt RAlt LWin RWin LCtrl RCtrl，另外可写任意键名（F13、\n";
    output << "# CapsLock 等，与 pin.hotkey 用同一张键名表）。设置界面只呈现那六个，\n";
    output << "# 这里写的其他键界面读得懂也不会覆盖掉。\n";
    output << "#\n";
    output << "# 勾选 Win 键会让程序额外安装一个键盘钩子，用来吞掉抬起时的开始菜单。\n";
    output << "# 只用 Alt / Ctrl 时不会安装。\n";
    output << "drag.enabled=" << (settings.drag.enabled ? "true" : "false") << "\n";
    output << "drag.modifiers=" << settings.drag.modifiers << "\n";
    output << "# 不参与拖动的应用。和 border.excluded_apps 分开：有些程序自己就用\n";
    output << "# Alt+拖动（Photoshop 等）。用「窗口拖动 -> 排除应用」勾选。\n";
    output << "drag.excluded_apps=" << EncodeList(settings.drag.excludedAppKeys) << "\n";
```

- [ ] **Step 5: 构建并跑测试**

```bash
cmake --build build --config Release --parallel
build\Release\windowmark_core_tests.exe
```

预期：零警告，全部通过。

- [ ] **Step 6: 往返自查**

用现有安装跑一次，打开设置面板什么都不改直接确定，然后比对 `settings.conf` 前后是否一致。
读写不对称最容易在这一步暴露——项目历史上「树控件复选框」那个数据丢失 bug 就是这么发现的。

- [ ] **Step 7: 提交**

```bash
git add include/windowmark/core/Settings.h src/core/Settings.cpp tests/core_tests.cpp
git commit -m "窗口拖动：配置项 drag.enabled / drag.modifiers / drag.excluded_apps"
```

---

### Task 4: 鼠标钩子与拖动（platform）

**Files:**
- Create: `src/platform/windows/WinDragBackend.h`、`WinDragBackend.cpp`
- Modify: `CMakeLists.txt`、`src/platform/windows/WinMain.cpp`

**Interfaces:**
- Consumes: Task 1–3 的 `DragModifiers` / `DragEdges` / `HitZone` / `ApplyDrag` / `Settings::drag`；`windowmark::win::IsEligibleTopLevelWindow`、`ToCoreRect` / `ToWinRect`（`WinUtil.h`）
- Produces:

```cpp
class WinDragBackend {
public:
    void Apply(const DragSettings& settings);   // 装/卸钩子并更新配置（含排除名单）
    void Shutdown();                            // 卸钩子，退出与「暂停所有」用
};
```

排除名单就在 `DragSettings` 里，backend 自己判断，**不需要回调、也不需要 Coordinator 新增
查询方法**。判断用的 key 与 `WinWindowBackend.cpp:309` 完全一致：

```cpp
identity.groupKey = LowerAscii(WideToUtf8(processPath));
```

即「小写化的可执行文件路径」。`LowerAscii` 目前在 `WinWindowBackend.cpp` 的匿名 namespace
里，需要移到 `WinUtil.h/.cpp` 供两边共用——**两份小写化逻辑必然漂移**，而 key 算不一致的
后果是排除名单静默失效（看起来一切正常，就是不生效），最难排查。

- [ ] **Step 1: 写 backend 头文件**

`src/platform/windows/WinDragBackend.h`：

```cpp
#pragma once

#include "windowmark/core/DragGeometry.h"
#include "windowmark/core/DragModifiers.h"
#include "windowmark/core/Settings.h"

#include <windows.h>

#include <functional>

namespace windowmark::win {

// 按住修饰键拖动窗口。全局低级鼠标钩子，必要时再加一个键盘钩子。
//
// 钩子是进程级的全局资源，所以这个类是单实例的：内部用一个文件级指针把回调转发到实例，
// 与 WinRenameDialog 用 GWLP_USERDATA 的做法同理——Win32 回调不带用户参数。
class WinDragBackend {
public:
    ~WinDragBackend();

    // 按配置装/卸钩子。enabled 为假或没有任何触发键时不装。
    // isExcluded 回答「这个窗口所属的应用在排除名单里吗」，由调用方接到 Coordinator。
    void Apply(const DragSettings& settings, std::function<bool(HWND)> isExcluded);

    // 卸掉全部钩子。退出和「暂停所有」都走这里。
    void Shutdown();

private:
    // ... 实现细节见 .cpp
};

} // namespace windowmark::win
```

- [ ] **Step 2: 写钩子的最短路径**

`WinDragBackend.cpp` 的核心是鼠标钩子回调。**它每一次鼠标移动都会被调用**，所以第一件事
必须是最便宜的判断：

```cpp
LRESULT CALLBACK WinDragBackend::MouseProc(int code, WPARAM wParam, LPARAM lParam) {
    // 快速路径：绝大多数时候这里什么都不该做。先看有没有正在拖动，再看修饰键——
    // 两个都是纯内存/纯寄存器判断，不查窗口、不算几何、不碰配置。
    //
    // 这是性能的全部要害：WH_MOUSE_LL 在安装它的线程上同步处理每一个鼠标事件，
    // 这里慢一点，整个系统的鼠标就跟着钝。
    if (code != HC_ACTION || g_instance == nullptr) {
        return CallNextHookEx(nullptr, code, wParam, lParam);
    }
    if (!g_instance->dragging_ && !g_instance->AnyModifierDown()) {
        return CallNextHookEx(nullptr, code, wParam, lParam);
    }
    return g_instance->HandleMouse(wParam, lParam);
}

// GetAsyncKeyState 的高位表示「当前按下」。对每个配置的键查一次，量级几十纳秒。
bool WinDragBackend::AnyModifierDown() const {
    for (const unsigned vk : modifiers_.keys) {
        if ((GetAsyncKeyState(static_cast<int>(vk)) & 0x8000) != 0) return true;
    }
    return false;
}
```

- [ ] **Step 3: 拖动状态机**

`HandleMouse` 处理四种事件，其余一律放行：

```cpp
LRESULT WinDragBackend::HandleMouse(WPARAM message, LPARAM lParam) {
    const auto* info = reinterpret_cast<const MSLLHOOKSTRUCT*>(lParam);

    switch (message) {
    case WM_LBUTTONDOWN:
    case WM_RBUTTONDOWN: {
        if (dragging_) break;
        HWND target = TargetWindowAt(info->pt);
        if (!target) break;

        target_ = target;
        startCursor_ = info->pt;
        startFrame_ = ToCoreRect(WindowRectOf(target));
        // 左键整窗移动，右键按九宫格决定拉哪几条边。
        edges_ = message == WM_LBUTTONDOWN ? DragEdges{true, true, true, true}
                                           : HitZone(startFrame_, info->pt.x, info->pt.y);
        usedRightButton_ = message == WM_RBUTTONDOWN;
        dragging_ = true;
        // 最大化的窗口先还原，否则 SetWindowPos 对它无效——与拖标题栏的行为一致。
        if (IsZoomed(target_)) RestoreForDrag();
        return 1;   // 吞掉：这一次按下已经被手势用掉了
    }

    case WM_MOUSEMOVE: {
        if (!dragging_) break;
        const Rect next = ApplyDrag(startFrame_, edges_,
                                    info->pt.x - startCursor_.x,
                                    info->pt.y - startCursor_.y,
                                    GetSystemMetrics(SM_CXMINTRACK),
                                    GetSystemMetrics(SM_CYMINTRACK));
        const RECT r = ToWinRect(next);
        SetWindowPos(target_, nullptr, r.left, r.top, r.right - r.left, r.bottom - r.top,
                     SWP_NOACTIVATE | SWP_NOZORDER | SWP_NOSENDCHANGING);
        return 1;
    }

    case WM_LBUTTONUP:
    case WM_RBUTTONUP: {
        if (!dragging_) break;
        dragging_ = false;
        target_ = nullptr;
        // 两个键的抬起都要吞，因为对应的按下已经被吞掉了——放行一个没有配对按下的抬起，
        // 应用的状态机会错乱。右键这一次尤其关键：放行它，应用会当成一次完整右击并弹出
        // 上下文菜单。
        return 1;
    }

    default:
        break;
    }
    return CallNextHookEx(nullptr, HC_ACTION, message, lParam);
}
```

`usedRightButton_` 目前只用来在 Task 5 里决定要不要置 `winConsumed_`；如果实现下来发现
它没有别的用处，删掉即可，不要为了对称保留一个没人读的字段。

「拖动中途松开修饰键继续拖」由状态机天然满足：`dragging_` 为真时快速路径不再检查修饰键。

- [ ] **Step 4: 目标窗口与排除名单**

```cpp
HWND WinDragBackend::TargetWindowAt(POINT pt) const {
    HWND hit = WindowFromPoint(pt);
    if (!hit) return nullptr;
    // WindowFromPoint 给的是子控件，要一路上溯到顶级窗口。
    HWND top = GetAncestor(hit, GA_ROOT);
    if (!top) return nullptr;
    // 桌面、任务栏这类不能拖；复用书签/边框那套资格判断，标准一致。
    if (!IsEligibleTopLevelWindow(top)) return nullptr;
    if (IsExcluded(top)) return nullptr;
    return top;
}

bool WinDragBackend::IsExcluded(HWND hwnd) const {
    if (excluded_.empty()) return false;
    DWORD pid = 0;
    GetWindowThreadProcessId(hwnd, &pid);
    // 与 WinWindowBackend.cpp:309 算 groupKey 的方式必须完全一致，否则排除名单会静默
    // 失效——看起来一切正常，就是不生效。
    const std::string key = LowerAscii(WideToUtf8(QueryProcessPath(pid)));
    return std::find(excluded_.begin(), excluded_.end(), key) != excluded_.end();
}
```

- [ ] **Step 5: 接进 WinMain**

`WinMain.cpp` 加 include 与实例，并在设置变化时调用 `Apply`：

```cpp
#include "WinDragBackend.h"
...
    windowmark::win::WinDragBackend dragBackend;

    const auto applyDrag = [&]() {
        dragBackend.Apply(coordinator.CurrentSettings().drag);
    };
```

启动时调用一次 `applyDrag()`，此后每次 `persist()` 之后也调用一次（设置改了要重新装/卸
钩子）。退出路径上调 `dragBackend.Shutdown()`。

先把 `LowerAscii` 从 `WinWindowBackend.cpp` 的匿名 namespace 移到 `WinUtil.h/.cpp`，
并让 `WinWindowBackend.cpp:309` 改用移过去的那一份——移动而不是复制，理由见上。

- [ ] **Step 6: 构建**

```bash
cmake --build build --config Release --parallel
python tools/check-escapes.py
```

（转义检查用 PowerShell 工具跑：`$env:PYTHONIOENCODING='utf-8'; python tools/check-escapes.py`）

- [ ] **Step 7: 实测钩子在空闲路径上的耗时**

这是 spec 里的硬指标。写一个探针（Write 工具写文件，PowerShell 跑）：在钩子的快速路径
两端取 `QueryPerformanceCounter`，累计 10000 次采样后把平均值和最大值写进日志。

**判据：空闲路径平均耗时应在 5µs 以内，超过 50µs 需要重新设计。**

- [ ] **Step 8: 实机验证拖动**

脚本用 `SendInput` 合成鼠标事件（**会真的移动光标，跑之前先跟用户打招呼**）：

| 检查 | 期望 |
|---|---|
| 按住右 Alt + 左键拖 | 窗口跟随光标，位移与光标位移一致 |
| 拖背景窗口 | 焦点不变（前台窗口仍是原来那个） |
| 拖最大化窗口 | 先还原再跟随 |
| 按住右 Alt + 右键拖右下角 | 向右下缩放，左上角不动 |
| 右键拖动结束 | **不弹出上下文菜单** |
| 拖动中途松开右 Alt | 拖动继续，直到松开鼠标键 |
| 不按修饰键的普通点击拖动 | 行为与平时完全一致 |
| 排除名单内的应用 | 按住修饰键拖动无反应，普通操作正常 |

- [ ] **Step 9: 提交**

```bash
git add src/platform/windows/WinDragBackend.h src/platform/windows/WinDragBackend.cpp src/platform/windows/WinMain.cpp CMakeLists.txt
git commit -m "窗口拖动：鼠标钩子与拖动状态机"
```

---

### Task 5: Win 键的键盘钩子（按需）

**Files:**
- Modify: `src/platform/windows/WinDragBackend.cpp`（`.h` 视需要）

**Interfaces:**
- Consumes: Task 4 的 `WinDragBackend` 内部状态

- [ ] **Step 1: 只在需要时安装**

`Apply` 里判断配置的键中有没有 `VK_LWIN`(0x5B) / `VK_RWIN`(0x5C)，有才装
`WH_KEYBOARD_LL`，没有就确保卸掉：

```cpp
    // 键盘钩子只为 Win 键而装。它比鼠标钩子更敏感——杀毒软件更关注，出错影响更大——
    // 所以只用 Alt / Ctrl 的用户不该承担它。
    const bool needsKeyboard = modifiers_.Contains(0x5B) || modifiers_.Contains(0x5C);
```

- [ ] **Step 2: 吞掉被手势用掉的 Win 抬起**

```cpp
LRESULT CALLBACK WinDragBackend::KeyboardProc(int code, WPARAM wParam, LPARAM lParam) {
    if (code != HC_ACTION || g_instance == nullptr) {
        return CallNextHookEx(nullptr, code, wParam, lParam);
    }
    const auto* info = reinterpret_cast<const KBDLLHOOKSTRUCT*>(lParam);
    const bool isWin = info->vkCode == VK_LWIN || info->vkCode == VK_RWIN;
    const bool isUp = wParam == WM_KEYUP || wParam == WM_SYSKEYUP;

    // Win 键按下再松开、中间什么都没发生时会弹出开始菜单。这一次拖动已经用掉了这个
    // Win 键，所以它的抬起不能再传下去。只吞「确实用掉了」的那一次：
    // winConsumed_ 在拖动真正开始时置位，抬起时清掉。
    if (g_instance->winConsumed_ && isWin && isUp) {
        g_instance->winConsumed_ = false;
        return 1;
    }
    return CallNextHookEx(nullptr, code, wParam, lParam);
}
```

`winConsumed_` 在 `HandleMouse` 判定开始拖动、且当时按住的是 Win 键时置为真。

- [ ] **Step 3: 构建并实机验证**

| 检查 | 期望 |
|---|---|
| 配置只有 `RAlt` 时启动 | **不安装**键盘钩子（日志确认） |
| 配置含 `LWin` 时启动 | 安装键盘钩子 |
| 勾 Win 后按住 Win 拖窗口，松开 | 窗口拖走了，**开始菜单不弹出** |
| 单独按一下 Win 键（没拖动） | 开始菜单**正常弹出**——只吞被用掉的那次 |

最后一条是关键：吞过头会把 Win 键整个废掉，那比不做这个功能更糟。

- [ ] **Step 4: 提交**

```bash
git add src/platform/windows/WinDragBackend.cpp src/platform/windows/WinDragBackend.h
git commit -m "窗口拖动：勾了 Win 键才装键盘钩子，吞掉被手势用掉的那次抬起"
```

---

### Task 6: 托盘菜单与设置对话框

**Files:**
- Modify: `src/platform/windows/WinControlWindow.h` / `.cpp`
- Create: `src/platform/windows/WinDragSettingsDialog.h` / `.cpp`
- Modify: `src/platform/windows/WinMain.cpp`、`CMakeLists.txt`
- Modify: `src/core/Coordinator.cpp`、`include/windowmark/core/Coordinator.h`（排除名单的快照/应用）

**Interfaces:**
- Consumes: Task 1 的 `PresetModifiers()`；Task 3 的 `Settings::drag`
- Produces:
  - `WinControlWindow::kToggleDragCommand = 1018`、`kDragSettingsCommand = 1019`、`kDragExcludeCommand = 1020`
  - `Handlers::onToggleDrag` / `onDragSettings` / `onDragExcludeApps`
  - `WinDragSettingsDialog::ShowModal(HWND owner, std::string& modifiers) -> bool`

- [ ] **Step 1: 顶层项与子菜单**

`WinControlWindow.cpp` 的 `ShowMenu`，在挂载「窗口置顶」之后、「剪贴板守护」之前插入：

```cpp
    HMENU dragging = CreatePopupMenu();
    if (dragging) {
        AppendMenuW(dragging, MF_STRING | (dragEnabled_ ? MF_CHECKED : MF_UNCHECKED),
                    kToggleDragCommand, L"启用窗口拖动");
        AppendMenuW(dragging, MF_STRING, kDragExcludeCommand, L"排除应用...");
        AppendMenuW(dragging, MF_SEPARATOR, 0, nullptr);
        AppendMenuW(dragging, MF_STRING, kDragSettingsCommand, L"拖动设置...");
        AppendMenuW(menu, MF_POPUP | (dragEnabled_ ? MF_CHECKED : MF_UNCHECKED),
                    reinterpret_cast<UINT_PTR>(dragging), L"窗口拖动");
    }
```

`dragEnabled_` 是新的成员，与 `bordersEnabled_` 等一样由 `WinMain` 在设置变化时同步进来；
照 `SetBordersEnabled` 的写法加一个 `SetDragEnabled`。

「暂停所有」的 `anythingOn` 判断要把 `dragEnabled_` 算进去，切换时也要一起切——它与书签/
边框/置顶同属 WindowMark 自己的功能。

- [ ] **Step 2: 六个复选框的设置对话框**

`WinDragSettingsDialog.cpp` 照 `WinConfigPathDialog.cpp` 的结构写（同样是自绘窗口类 +
模态循环 + `S()` DPI 缩放）。控件就是 `PresetModifiers()` 的六个复选框，两列三行：

```cpp
void Build() {
    Add(L"STATIC", L"按住下面任意一个键，再用鼠标拖动窗口：\n左键拖 = 移动，右键拖 = 缩放",
        0, 14, 12, 420, 40, kNoteId);

    const auto& presets = PresetModifiers();
    for (std::size_t i = 0; i < presets.size(); ++i) {
        const int col = static_cast<int>(i) % 2;
        const int row = static_cast<int>(i) / 2;
        HWND box = Add(L"BUTTON", presets[i].label,
                       WS_TABSTOP | BS_AUTOCHECKBOX | (i == 0 ? WS_GROUP : 0),
                       14 + col * 210, 62 + row * 30, 200, 26,
                       kFirstPresetId + static_cast<int>(i));
        CheckDlgButton(hwnd_, kFirstPresetId + static_cast<int>(i),
                       current_.Contains(presets[i].vk) ? BST_CHECKED : BST_UNCHECKED);
        (void)box;
    }

    Add(L"STATIC",
        L"配置文件里还可以写其他按键（如 F13），这里不显示但不会被覆盖。\n"
        L"勾选 Win 键会额外安装一个键盘钩子，用来吞掉抬起时的开始菜单。",
        0, 14, 158, 420, 40, kHintId);

    Add(L"BUTTON", L"确定", WS_TABSTOP | BS_DEFPUSHBUTTON, 250, 210, 84, 28, kOkId);
    Add(L"BUTTON", L"取消", WS_TABSTOP, 344, 210, 84, 28, kCancelId);
}
```

确定时，把六个复选框的状态与**原有配置里的非预设键**合并后写回：

```cpp
case kOkId: {
    // 只改六个预设的勾选状态，配置文件里写的其他键原样保留——界面不显示它们，
    // 更不该悄悄删掉它们。
    DragModifiers next;
    for (const unsigned vk : current_.keys) {
        const bool isPreset = std::any_of(
            PresetModifiers().begin(), PresetModifiers().end(),
            [vk](const PresetModifier& p) { return p.vk == vk; });
        if (!isPreset) next.keys.push_back(vk);
    }
    const auto& presets = PresetModifiers();
    for (std::size_t i = 0; i < presets.size(); ++i) {
        if (IsDlgButtonChecked(hwnd_, kFirstPresetId + static_cast<int>(i)) == BST_CHECKED) {
            if (!next.Contains(presets[i].vk)) next.keys.push_back(presets[i].vk);
        }
    }
    result_ = FormatDragModifiers(next);
    accepted_ = true;
    DestroyWindow(hwnd_);
    return 0;
}
```

**`WM_DESTROY` 里写 `hwnd_ = nullptr;`，不要 `PostQuitMessage`** —— 这个模态循环嵌在托盘
应用自己的消息循环里，发 `WM_QUIT` 会把整个应用带走。`WinRenameDialog.cpp` 和
`WinConfigPathDialog.cpp` 都有同样的注释。

- [ ] **Step 3: 排除应用面板**

复用 `WinSelectionDialog`，照 `excludeBorderApps` 那个 lambda 写一个 `excludeDragApps`：
标题改成「WindowMark - 排除不参与拖动的应用/窗口」，说明文字改成「勾上 = 按住修饰键也不
拖动它」，`checkedMeansExcluded = true`。

`Coordinator` 需要一对新方法，照 `BorderSelectionSnapshot` / `ApplyBorderSelection` 写
`DragSelectionSnapshot` / `ApplyDragSelection`。

- [ ] **Step 4: 接线与自检**

三个 handler 接到 `WinMain`；`WinControlWindow.cpp` 的 `wired` 自检数组加三行：

```cpp
        {L"onToggleDrag", static_cast<bool>(handlers_.onToggleDrag)},
        {L"onDragSettings", static_cast<bool>(handlers_.onDragSettings)},
        {L"onDragExcludeApps", static_cast<bool>(handlers_.onDragExcludeApps)},
```

这个自检存在的理由：空的 `std::function` 调用时什么都不发生，「忘了接线」是完全无症状的
——项目历史上「全部取消置顶」就这么静默失效过。

- [ ] **Step 5: 构建、装机、验证**

```bash
cmake --build build --config Release --parallel
powershell -NoProfile -ExecutionPolicy Bypass -File .\reinstall.ps1
```

| 检查 | 期望 |
|---|---|
| 顶层「窗口拖动」在「窗口置顶」下面 | 启用时有对勾 |
| 子菜单三项齐全 | 启用窗口拖动 / 排除应用... / 拖动设置... |
| 拖动设置里勾选变化后确定 | `drag.modifiers` 按固定顺序写回 |
| 配置文件里先写 `RAlt|F13`，打开设置直接确定 | **F13 仍在**，没被界面抹掉 |
| 「暂停所有」 | 拖动跟着停，再点恢复 |
| 菜单宽度 | 用 `#32768` 探针量一次，记录变化 |

倒数第二条尤其要测：界面只显示六个预设，很容易在写回时把用户手写的额外键丢掉。

- [ ] **Step 6: 提交**

```bash
git add src/platform/windows/WinDragSettingsDialog.h src/platform/windows/WinDragSettingsDialog.cpp src/platform/windows/WinControlWindow.h src/platform/windows/WinControlWindow.cpp src/platform/windows/WinMain.cpp src/core/Coordinator.cpp include/windowmark/core/Coordinator.h CMakeLists.txt
git commit -m "窗口拖动：托盘子菜单、六个修饰键复选框、排除应用"
```

---

### Task 7: 文档、版本、发布

**Files:**
- Modify: `src/shared/AppIdentity.h`、`CMakeLists.txt`（版本 → `0.5.0`）
- Modify: `README.md`、`CHANGELOG.md`、`VALIDATION.md`、`docs/Windows开发避坑规则.md`
- Modify: `publish_release.bat`

- [ ] **Step 1: 升版本到 0.5.0**

窗口拖动是新功能而不是修复，按 README 的版本策略「Fixes go to v0.4.x; larger new features
go to the next minor line」，走 `0.5.0`。`AppIdentity.h` 的 `kProductVersion` 与
`CMakeLists.txt` 的 `project(...)` 同步改，两处不一致会在 configure 阶段直接失败。

- [ ] **Step 2: README 加「窗口拖动」一节**

必须写清楚五件事：

- 左键拖=移动、右键拖=缩放，右键按九宫格决定拉哪条边
- 触发键默认只有右 Alt，六个复选框可选，配置文件可补任意键
- **勾 Win 键会额外装键盘钩子**，只用 Alt/Ctrl 时不装
- 有些程序自己用 Alt+拖动（Photoshop 等），用「排除应用」解决
- **代码全部自己实现，没有引入 AltSnap 的任何代码**——它是 GPL-3.0，而 WindowMark 是 MIT

同时更新托盘菜单示意图。

- [ ] **Step 3: CHANGELOG 加 v0.5.0 段**

写清楚功能、触发键设计（任一键而非组合）、Win 键按需钩子的理由、以及许可证上的取舍。

- [ ] **Step 4: VALIDATION 记录本轮实测**

core 单测覆盖的项、钩子空闲路径耗时实测值、Task 4/5/6 的实机验证表、菜单宽度变化。

- [ ] **Step 5: 避坑规则加一条**

```markdown
### 低级钩子：快速路径决定一切

- `WH_MOUSE_LL` 在安装它的线程上**同步**处理每一个鼠标事件。回调里做的任何事都直接加在
  系统的鼠标延迟上。第一件事必须是最便宜的判断，不满足立刻 `CallNextHookEx`。
- **吞掉按下就必须吞掉抬起**，否则应用会收到没有配对按下的抬起事件：右键抬起放行会弹出
  上下文菜单，Win 键抬起放行会弹出开始菜单。
- 反过来，**只吞「确实被手势用掉」的那一次**。吞过头比不做更糟——把 Win 键整个废掉，
  用户会以为系统坏了。
- 键盘钩子比鼠标钩子更敏感（杀毒软件更关注），能不装就不装，按需安装。
```

- [ ] **Step 6: 全量构建 + 单测 + 装机**

```bash
cmake --build build --config Release --parallel
build\Release\windowmark_core_tests.exe
build\Release\windowmark_autostart_tests.exe
powershell -NoProfile -ExecutionPolicy Bypass -File .\reinstall.ps1
```

转义检查用 PowerShell 跑。

- [ ] **Step 7: 打包、提交、发布**

按 v0.4.7 的流程：`dist/WindowMark-v0.5.0-win64/` 装齐五个 exe 加 README/CHANGELOG/**LICENSE**
加 tools，压包，校验哈希，提交打 tag 推送，`gh release create`，最后下载回来比对 SHA256。

`publish_release.bat` 的 `TAG` / `ZIP` / `NOTES` 三处改成 `v0.5.0`。

---

## 风险与回滚

| 风险 | 影响 | 应对 |
|---|---|---|
| 钩子拖慢全系统鼠标 | 所有程序手感变差 | 快速路径优先；Task 4 Step 7 实测耗时，超 50µs 重新设计 |
| 吞事件吞过头 | Win 键或右键菜单整个失效 | 只吞被手势用掉的那一次；Task 5 Step 3 专门验「没拖动时 Win 键正常」 |
| 与应用自身的 Alt+拖动冲突 | Photoshop 等无法正常使用 | 排除名单；文档写明 |
| 设置界面抹掉配置文件里的额外键 | 用户手写的配置静默丢失 | 确定时合并而非覆盖；Task 6 Step 5 专门验 |
| 左右修饰键没分开 | 按左 Alt 也触发，与系统行为冲突 | Task 1 单测 + 变异测试确认拦得住 |

单任务出问题 `git revert` 该任务的提交即可。Task 1–3 是纯 core 新增，不影响既有功能；
Task 4 起才装钩子，且 `drag.enabled` 默认为 false，未开启时代码路径完全不生效。
