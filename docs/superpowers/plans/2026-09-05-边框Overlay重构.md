# 边框 Overlay 重构实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把边框从「每个窗口一个 HWND 去抢全局 z 序」换成「每个显示器一个透明 topmost overlay + 遮挡裁剪」，让「边框排第几」的时序问题变成「哪几段该画」的几何问题。

**Architecture:** Z 序只读不写。一次事件触发一次快照（foreground + z 序 + DWM 边界 + cloaked + maximized），同一帧只认这一份；按 z 序从上往下扫，非激活窗口的边框环减去上方所有窗口的并集，激活窗口的边框不裁剪、最后画；结果画到每个显示器一个的透明 overlay 上。

**Tech Stack:** C++20、CMake、Win32、Direct2D、`UpdateLayeredWindowIndirect`

## Global Constraints

- 语言标准 C++20（`target_compile_features(windowmark_core PUBLIC cxx_std_20)`）
- 编译零警告：MSVC `/W4 /WX /permissive-`，GCC/Clang `-Wall -Wextra -Wpedantic -Werror`
- 纯几何逻辑放 `windowmark_core`，不引用任何 Win32 类型（照 `BorderGeometry.h` 的模式），平台层把矩形查好传进去
- 单元测试用 `tests/core_tests.cpp` 里的 `CHECK(...)` 宏，不引入测试框架
- 窗口边界一律用 `DWMWA_EXTENDED_FRAME_BOUNDS`，失败才退回 `GetWindowRect`
- 最大化窗口不画边框（置顶窗口例外），这条行为在 v0.4.9-rc1 已生效，重构后必须保持
- 每次改完跑 `reinstall.ps1`，它会在有任何警告时拒绝安装

---

### Task 1: 性能验证（gate，不达标就停）

这是**关卡任务**。现有实现拖动时只做一次 `SetWindowPos`、不重绘，实测中位数 3.0ms、p99 15.7ms、一帧内 100%。Overlay 方案每帧都要重绘并提交位图，全屏 2048×1152×4 ≈ 9.4MB。**必须先证明脏矩形提交能把开销压到同量级**，否则整个渲染策略要重想 —— 那时候代码已经铺开就来不及了。

**Files:**
- Create: `tools/overlay-bench/OverlayBench.cpp`
- Modify: `CMakeLists.txt`（在 `windowmark_core_tests` 那段之后追加）

**Interfaces:**
- Consumes: 无（自包含的独立 exe）
- Produces: 无（一次性验证工具，结论写进本计划的验收记录）

- [ ] **Step 1: 写基准程序**

创建 `tools/overlay-bench/OverlayBench.cpp`：

```cpp
// Overlay 方案的性能关卡：一个覆盖整屏的分层窗口，反复只更新四条细带，
// 量每次提交要多久。
//
// 要回答的问题只有一个：用 UpdateLayeredWindowIndirect 的脏矩形，把一圈边框的
// 变化提交上去，能不能压到和现在「一次 SetWindowPos 移动一个小窗口」同量级
// （中位数 3.0ms / p99 15.7ms）。压不到，Overlay 方案就得换渲染策略。
#include <windows.h>

#include <algorithm>
#include <cstdio>
#include <vector>

namespace {

LARGE_INTEGER g_freq{};

double NowMs() {
    LARGE_INTEGER now{};
    QueryPerformanceCounter(&now);
    return static_cast<double>(now.QuadPart) * 1000.0 / static_cast<double>(g_freq.QuadPart);
}

LRESULT CALLBACK OverlayProc(HWND hwnd, UINT msg, WPARAM wParam, LPARAM lParam) {
    if (msg == WM_DESTROY) {
        PostQuitMessage(0);
        return 0;
    }
    return DefWindowProcW(hwnd, msg, wParam, lParam);
}

// 32bpp 自上而下的 DIB，配 ULW_ALPHA 需要预乘 alpha。
struct Surface {
    HDC dc{};
    HBITMAP bitmap{};
    HBITMAP old{};
    void* bits{};
    int width{};
    int height{};

    bool Create(int w, int h) {
        HDC screen = GetDC(nullptr);
        dc = CreateCompatibleDC(screen);
        ReleaseDC(nullptr, screen);
        if (!dc) return false;

        BITMAPINFO info{};
        info.bmiHeader.biSize = sizeof(info.bmiHeader);
        info.bmiHeader.biWidth = w;
        info.bmiHeader.biHeight = -h;   // 负数 = 自上而下
        info.bmiHeader.biPlanes = 1;
        info.bmiHeader.biBitCount = 32;
        info.bmiHeader.biCompression = BI_RGB;
        bitmap = CreateDIBSection(dc, &info, DIB_RGB_COLORS, &bits, nullptr, 0);
        if (!bitmap) return false;
        old = static_cast<HBITMAP>(SelectObject(dc, bitmap));
        width = w;
        height = h;
        return true;
    }

    void Destroy() {
        if (dc && old) SelectObject(dc, old);
        if (bitmap) DeleteObject(bitmap);
        if (dc) DeleteDC(dc);
    }

    // 直接写像素：这个基准量的是提交开销，不是绘制质量，所以不引入 D2D。
    void FillRect(const RECT& r, unsigned argb) {
        auto* px = static_cast<unsigned*>(bits);
        const int x0 = std::max<int>(0, r.left);
        const int y0 = std::max<int>(0, r.top);
        const int x1 = std::min<int>(width, r.right);
        const int y1 = std::min<int>(height, r.bottom);
        for (int y = y0; y < y1; ++y) {
            unsigned* row = px + static_cast<size_t>(y) * width;
            for (int x = x0; x < x1; ++x) row[x] = argb;
        }
    }
};

}  // namespace

int wmain(int argc, wchar_t** argv) {
    QueryPerformanceFrequency(&g_freq);
    SetProcessDpiAwarenessContext(DPI_AWARENESS_CONTEXT_PER_MONITOR_AWARE_V2);

    const int rounds = argc > 1 ? _wtoi(argv[1]) : 300;

    WNDCLASSEXW wc{};
    wc.cbSize = sizeof(wc);
    wc.lpfnWndProc = OverlayProc;
    wc.hInstance = GetModuleHandleW(nullptr);
    wc.lpszClassName = L"WindowMark.OverlayBench";
    RegisterClassExW(&wc);

    const int sw = GetSystemMetrics(SM_CXSCREEN);
    const int sh = GetSystemMetrics(SM_CYSCREEN);

    HWND overlay = CreateWindowExW(
        WS_EX_LAYERED | WS_EX_TRANSPARENT | WS_EX_TOOLWINDOW | WS_EX_NOACTIVATE |
            WS_EX_TOPMOST,
        L"WindowMark.OverlayBench", L"", WS_POPUP,
        0, 0, sw, sh, nullptr, nullptr, wc.hInstance, nullptr);
    if (!overlay) {
        std::printf("创建 overlay 失败: %lu\n", GetLastError());
        return 1;
    }
    ShowWindow(overlay, SW_SHOWNOACTIVATE);

    Surface surface;
    if (!surface.Create(sw, sh)) {
        std::printf("创建 DIB 失败\n");
        return 1;
    }

    std::vector<double> fullTimes;
    std::vector<double> dirtyTimes;
    fullTimes.reserve(rounds);
    dirtyTimes.reserve(rounds);

    const int stroke = 4;
    RECT prev{};

    for (int i = 0; i < rounds; ++i) {
        // 模拟一个 1200x800 的窗口在屏幕上移动
        const int x = 100 + (i % 200) * 3;
        const int y = 80 + (i % 100) * 2;
        RECT frame{x, y, x + 1200, y + 800};

        // 擦掉上一圈，画新的一圈
        if (i > 0) {
            const RECT strips[4] = {
                {prev.left, prev.top, prev.right, prev.top + stroke},
                {prev.left, prev.bottom - stroke, prev.right, prev.bottom},
                {prev.left, prev.top, prev.left + stroke, prev.bottom},
                {prev.right - stroke, prev.top, prev.right, prev.bottom},
            };
            for (const RECT& s : strips) surface.FillRect(s, 0x00000000);
        }
        const RECT strips[4] = {
            {frame.left, frame.top, frame.right, frame.top + stroke},
            {frame.left, frame.bottom - stroke, frame.right, frame.bottom},
            {frame.left, frame.top, frame.left + stroke, frame.bottom},
            {frame.right - stroke, frame.top, frame.right, frame.bottom},
        };
        for (const RECT& s : strips) surface.FillRect(s, 0xFF6274E7);

        POINT src{0, 0};
        SIZE size{sw, sh};
        POINT dst{0, 0};
        BLENDFUNCTION blend{};
        blend.BlendOp = AC_SRC_OVER;
        blend.SourceConstantAlpha = 255;
        blend.AlphaFormat = AC_SRC_ALPHA;

        // 变化区域 = 旧圈与新圈的并集，外扩 stroke 保证覆盖
        RECT dirty = frame;
        if (i > 0) {
            dirty.left = std::min(dirty.left, prev.left);
            dirty.top = std::min(dirty.top, prev.top);
            dirty.right = std::max(dirty.right, prev.right);
            dirty.bottom = std::max(dirty.bottom, prev.bottom);
        }
        InflateRect(&dirty, stroke, stroke);

        UPDATELAYEREDWINDOWINFO info{};
        info.cbSize = sizeof(info);
        info.pptSrc = &src;
        info.psize = &size;
        info.hdcSrc = surface.dc;
        info.pptDst = &dst;
        info.pblend = &blend;
        info.dwFlags = ULW_ALPHA;

        // 先量脏矩形提交
        info.prcDirty = &dirty;
        const double t0 = NowMs();
        UpdateLayeredWindowIndirect(overlay, &info);
        dirtyTimes.push_back(NowMs() - t0);

        // 再量整屏提交，作为对照
        info.prcDirty = nullptr;
        const double t1 = NowMs();
        UpdateLayeredWindowIndirect(overlay, &info);
        fullTimes.push_back(NowMs() - t1);

        prev = frame;
        Sleep(8);
    }

    surface.Destroy();
    DestroyWindow(overlay);

    auto report = [](const char* name, std::vector<double> v) {
        std::sort(v.begin(), v.end());
        auto pct = [&v](double p) {
            const size_t i = std::min(v.size() - 1,
                                      static_cast<size_t>(v.size() * p / 100.0));
            return v[i];
        };
        const size_t oneFrame =
            static_cast<size_t>(std::count_if(v.begin(), v.end(),
                                              [](double d) { return d <= 16.7; }));
        std::printf("%-14s 中位数 %.2f ms  p90 %.2f ms  p99 %.2f ms  最慢 %.2f ms  "
                    "一帧内 %zu/%zu (%.0f%%)\n",
                    name, pct(50), pct(90), pct(99), v.back(), oneFrame, v.size(),
                    100.0 * static_cast<double>(oneFrame) / static_cast<double>(v.size()));
    };

    std::printf("Overlay 提交开销（%d 轮，全屏 %dx%d）\n\n", rounds, sw, sh);
    report("脏矩形提交", dirtyTimes);
    report("整屏提交", fullTimes);
    std::printf("\n现有基线（每窗口一个边框、纯移动不重绘）：\n");
    std::printf("               中位数 3.00 ms  p99 15.70 ms  一帧内 100%%\n");
    return 0;
}
```

- [ ] **Step 2: 加进构建**

在 `CMakeLists.txt` 里 `add_test(NAME windowmark_core_tests ...)` 那一行之后追加：

```cmake
if(WIN32)
    # Overlay 方案的性能关卡工具。不参与安装包，只在开发时手工跑。
    add_executable(OverlayBench WIN32 tools/overlay-bench/OverlayBench.cpp)
    set_target_properties(OverlayBench PROPERTIES WIN32_EXECUTABLE FALSE)
    target_link_libraries(OverlayBench PRIVATE user32 gdi32)
endif()
```

- [ ] **Step 3: 构建**

Run: `cmake --build build --config Release --target OverlayBench`
Expected: 零警告编译通过（`/W4 /WX` 生效）

- [ ] **Step 4: 跑基准，记录数字**

Run: `build\Release\OverlayBench.exe 300`

Expected: 打出脏矩形与整屏两组数字。

**判据（关卡）：**
- 脏矩形提交中位数 ≤ 6ms 且 p99 ≤ 20ms → **通过**，继续 Task 2
- 超出但整屏提交也在同量级 → 记录下来，继续，但 Task 5 必须用脏矩形路径
- 脏矩形中位数 > 10ms → **停下**，把数字贴到本计划末尾的验收记录里，重新讨论渲染策略（候选：D2D + DXGI 翻转链、或退回「只画激活窗口」的兼容模式）

- [ ] **Step 5: 把结论写进计划**

在本文件末尾「验收记录」一节填入实际数字和结论。

- [ ] **Step 6: 提交**

```bash
git add tools/overlay-bench/OverlayBench.cpp CMakeLists.txt docs/superpowers/plans/2026-09-05-边框Overlay重构.md
git commit -m "bench: Overlay 提交开销的性能关卡"
```

---

### Task 2: 遮挡裁剪的纯几何函数

**Files:**
- Create: `include/windowmark/core/BorderOcclusion.h`
- Create: `src/core/BorderOcclusion.cpp`
- Modify: `CMakeLists.txt:11`（`add_library(windowmark_core STATIC` 的源文件列表）
- Test: `tests/core_tests.cpp`

**Interfaces:**
- Consumes: `windowmark::Rect`（`include/windowmark/core/Types.h`）
- Produces:
  - `std::vector<Rect> SubtractRect(const Rect& from, const Rect& hole)`
  - `std::vector<Rect> BorderRingSegments(const Rect& outer, const Rect& inner)`
  - `std::vector<Rect> VisibleBorderSegments(const Rect& outer, const Rect& inner, const std::vector<Rect>& occluders)`

- [ ] **Step 1: 写失败的测试**

在 `tests/core_tests.cpp` 的 `#include` 区加一行 `#include "windowmark/core/BorderOcclusion.h"`，并在 `main()` 里现有测试调用之后加：

```cpp
void TestBorderOcclusion() {
    // 一、矩形减矩形
    {
        // 完全不重叠：原样返回
        const auto parts = SubtractRect({0, 0, 10, 10}, {20, 20, 30, 30});
        CHECK(parts.size() == 1);
        CHECK(parts[0].left == 0 && parts[0].right == 10);
    }
    {
        // 完全被盖住：什么都不剩
        const auto parts = SubtractRect({10, 10, 20, 20}, {0, 0, 100, 100});
        CHECK(parts.empty());
    }
    {
        // 从左边咬掉一半
        const auto parts = SubtractRect({0, 0, 100, 10}, {0, 0, 50, 10});
        CHECK(parts.size() == 1);
        CHECK(parts[0].left == 50 && parts[0].right == 100);
    }
    {
        // 从正中间挖一个洞，横条被切成左右两段
        const auto parts = SubtractRect({0, 0, 100, 10}, {40, 0, 60, 10});
        CHECK(parts.size() == 2);
        CHECK(parts[0].right == 40);
        CHECK(parts[1].left == 60);
    }

    // 二、边框环拆成四条边
    {
        const auto ring = BorderRingSegments({0, 0, 100, 50}, {4, 4, 96, 46});
        CHECK(ring.size() == 4);
        // 上边：整宽，高 4
        CHECK(ring[0].left == 0 && ring[0].top == 0);
        CHECK(ring[0].right == 100 && ring[0].bottom == 4);
        // 下边
        CHECK(ring[1].top == 46 && ring[1].bottom == 50);
        // 左边：只占中间那段高度，不和上下重复
        CHECK(ring[2].left == 0 && ring[2].right == 4);
        CHECK(ring[2].top == 4 && ring[2].bottom == 46);
        // 右边
        CHECK(ring[3].left == 96 && ring[3].right == 100);
    }

    // 三、整条链路：一圈边框被一个窗口盖住右半边
    {
        const auto segs = VisibleBorderSegments({0, 0, 100, 50}, {4, 4, 96, 46},
                                                {{50, 0, 200, 200}});
        // 右边整条没了，上下各剩左半段，左边完好
        for (const auto& s : segs) CHECK(s.right <= 50);
        CHECK(!segs.empty());
    }
    {
        // 没有遮挡物：四条边原样
        const auto segs = VisibleBorderSegments({0, 0, 100, 50}, {4, 4, 96, 46}, {});
        CHECK(segs.size() == 4);
    }
    {
        // 被完全盖住：一段都不剩
        const auto segs = VisibleBorderSegments({0, 0, 100, 50}, {4, 4, 96, 46},
                                                {{-10, -10, 200, 200}});
        CHECK(segs.empty());
    }
    std::cout << "BorderOcclusion tests passed.\n";
}
```

并在 `main()` 末尾（现有 `std::cout << "WindowMark core tests passed.\n";` 之前）调用 `TestBorderOcclusion();`。

- [ ] **Step 2: 跑测试，确认编译失败**

Run: `cmake --build build --config Release --target windowmark_core_tests`
Expected: FAIL —— 找不到 `windowmark/core/BorderOcclusion.h`

- [ ] **Step 3: 写头文件**

创建 `include/windowmark/core/BorderOcclusion.h`：

```cpp
#pragma once

#include "windowmark/core/Types.h"

#include <vector>

namespace windowmark {

// 从 from 里挖掉 hole，返回剩下的部分（最多四块）。
//
// 用矩形分解而不是 Region：边框是四条细带，减法产生的碎片很少，而 Region 要跨进
// GDI、每次都分配内核对象。这里全是纯算术，可以脱离 Windows 测试。
[[nodiscard]] std::vector<Rect> SubtractRect(const Rect& from, const Rect& hole);

// 把边框环拆成四条互不重叠的边：上、下、左、右。
//
// outer 是边框的外矩形，inner 是它围住的那个洞（也就是窗口自己的边界）。左右两条
// 边只占中间那段高度，避免和上下两条在角上重叠——重叠会让同一块像素被画两次，
// 半透明色下会显出更深的角。
[[nodiscard]] std::vector<Rect> BorderRingSegments(const Rect& outer, const Rect& inner);

// 边框环减去所有遮挡物，返回还看得见的段。
//
// 这是整个 Overlay 方案的核心：非激活窗口的边框不再靠 z 序去「排在正确的位置」，
// 而是直接把被上方窗口盖住的那几段**不画**。激活窗口不走这个函数，它的边框完整画、
// 最后画。
[[nodiscard]] std::vector<Rect> VisibleBorderSegments(const Rect& outer, const Rect& inner,
                                                      const std::vector<Rect>& occluders);

} // namespace windowmark
```

- [ ] **Step 4: 写实现**

创建 `src/core/BorderOcclusion.cpp`：

```cpp
#include "windowmark/core/BorderOcclusion.h"

#include <algorithm>

namespace windowmark {
namespace {

[[nodiscard]] bool IsEmpty(const Rect& r) {
    return r.right <= r.left || r.bottom <= r.top;
}

[[nodiscard]] bool Intersects(const Rect& a, const Rect& b) {
    return a.left < b.right && b.left < a.right && a.top < b.bottom && b.top < a.bottom;
}

void Push(std::vector<Rect>& out, const Rect& r) {
    if (!IsEmpty(r)) out.push_back(r);
}

} // namespace

std::vector<Rect> SubtractRect(const Rect& from, const Rect& hole) {
    std::vector<Rect> out;
    if (IsEmpty(from)) return out;
    if (!Intersects(from, hole)) {
        out.push_back(from);
        return out;
    }

    // 按上、下、左、右四刀切。先切上下整条，再切左右中间那段，切出来的块互不重叠。
    Push(out, Rect{from.left, from.top, from.right, std::min(from.bottom, hole.top)});
    Push(out, Rect{from.left, std::max(from.top, hole.bottom), from.right, from.bottom});

    const int midTop = std::max(from.top, hole.top);
    const int midBottom = std::min(from.bottom, hole.bottom);
    if (midTop < midBottom) {
        Push(out, Rect{from.left, midTop, std::min(from.right, hole.left), midBottom});
        Push(out, Rect{std::max(from.left, hole.right), midTop, from.right, midBottom});
    }
    return out;
}

std::vector<Rect> BorderRingSegments(const Rect& outer, const Rect& inner) {
    std::vector<Rect> ring;
    Push(ring, Rect{outer.left, outer.top, outer.right, inner.top});        // 上
    Push(ring, Rect{outer.left, inner.bottom, outer.right, outer.bottom});  // 下
    Push(ring, Rect{outer.left, inner.top, inner.left, inner.bottom});      // 左
    Push(ring, Rect{inner.right, inner.top, outer.right, inner.bottom});    // 右
    return ring;
}

std::vector<Rect> VisibleBorderSegments(const Rect& outer, const Rect& inner,
                                        const std::vector<Rect>& occluders) {
    std::vector<Rect> segments = BorderRingSegments(outer, inner);
    for (const Rect& occluder : occluders) {
        if (segments.empty()) break;
        std::vector<Rect> next;
        next.reserve(segments.size());
        for (const Rect& seg : segments) {
            for (const Rect& part : SubtractRect(seg, occluder)) next.push_back(part);
        }
        segments.swap(next);
    }
    return segments;
}

} // namespace windowmark
```

- [ ] **Step 5: 加进构建**

`CMakeLists.txt` 第 11 行 `src/core/BorderGeometry.cpp` 之后加一行：

```cmake
    src/core/BorderOcclusion.cpp
```

- [ ] **Step 6: 跑测试，确认通过**

Run: `cmake --build build --config Release --target windowmark_core_tests && build\Release\windowmark_core_tests.exe`
Expected: 打出 `BorderOcclusion tests passed.` 与 `WindowMark core tests passed.`

- [ ] **Step 7: 提交**

```bash
git add include/windowmark/core/BorderOcclusion.h src/core/BorderOcclusion.cpp tests/core_tests.cpp CMakeLists.txt
git commit -m "feat: 边框遮挡裁剪的纯几何函数"
```

---

### Task 3: 桌面快照

**Files:**
- Create: `src/platform/windows/WinDesktopSnapshot.h`
- Create: `src/platform/windows/WinDesktopSnapshot.cpp`
- Modify: `CMakeLists.txt`（Windows 那段的 `WindowMark` 源文件列表）

**Interfaces:**
- Consumes: 无
- Produces:
  - `struct SnapshotWindow { HWND hwnd; RECT frame; bool cloaked; bool maximized; bool minimized; };`
  - `struct DesktopSnapshot { HWND foreground; std::vector<SnapshotWindow> windows; };`（`windows` 按 z 序**从上到下**排列）
  - `DesktopSnapshot CaptureDesktop();`

- [ ] **Step 1: 写头文件**

创建 `src/platform/windows/WinDesktopSnapshot.h`：

```cpp
#pragma once

#include <windows.h>

#include <vector>

namespace windowmark::win {

// 一个窗口在某一瞬间的样子。
struct SnapshotWindow {
    HWND hwnd{};
    RECT frame{};          // DWMWA_EXTENDED_FRAME_BOUNDS，取不到才退回 GetWindowRect
    bool cloaked{false};   // 被 DWM 藏起来（多半是在别的虚拟桌面）
    bool maximized{false};
    bool minimized{false};
};

// 整个桌面在某一瞬间的样子。windows 按 z 序**从上到下**排列。
//
// 存在的理由是消灭 split-brain：以前 Coordinator 记着一份事件驱动的 active，平台层
// 又实时调 GetForegroundWindow()，两者在焦点切换途中会分家，同一个窗口在相邻两次
// 调用里走不同分支。现在一帧只取一次快照，那一帧里所有判断都只认这一份。
struct DesktopSnapshot {
    HWND foreground{};
    std::vector<SnapshotWindow> windows;
};

// 现场取一份快照。WinEvent 钩子只负责喊「脏了」，值一律现取。
[[nodiscard]] DesktopSnapshot CaptureDesktop();

} // namespace windowmark::win
```

- [ ] **Step 2: 写实现**

创建 `src/platform/windows/WinDesktopSnapshot.cpp`：

```cpp
#include "WinDesktopSnapshot.h"

#include <dwmapi.h>

namespace windowmark::win {
namespace {

// 遍历 z 序的上限。桌面上挂着数百个隐藏的顶级窗口（实测单个 Excel 窗口上方就压着
// 144 个），这个上限只是防止病态 z 序把线程转死。
constexpr int kZOrderLimit = 4096;

[[nodiscard]] bool IsCloaked(HWND hwnd) {
    // 切到别的虚拟桌面时，那边的窗口 IsWindowVisible 仍然返回 true，只看它会让
    // z 序和遮挡计算全乱。DWMWA_CLOAKED 才问得出「DWM 到底画不画它」。
    int cloaked = 0;
    if (FAILED(DwmGetWindowAttribute(hwnd, DWMWA_CLOAKED, &cloaked, sizeof(cloaked)))) {
        return false;
    }
    return cloaked != 0;
}

[[nodiscard]] RECT FrameOf(HWND hwnd) {
    RECT frame{};
    // GetWindowRect 会把不可见的 resize border 算进去（本机 125% 下实测 8px），
    // 还有 DPI virtualization；DWM 的扩展边界才是实际画出来的那一圈。
    if (SUCCEEDED(DwmGetWindowAttribute(hwnd, DWMWA_EXTENDED_FRAME_BOUNDS, &frame,
                                        sizeof(frame)))) {
        return frame;
    }
    GetWindowRect(hwnd, &frame);
    return frame;
}

} // namespace

DesktopSnapshot CaptureDesktop() {
    DesktopSnapshot snapshot;
    snapshot.foreground = GetForegroundWindow();

    HWND hwnd = GetTopWindow(nullptr);
    for (int step = 0; step < kZOrderLimit && hwnd != nullptr; ++step) {
        if (IsWindowVisible(hwnd)) {
            SnapshotWindow entry;
            entry.hwnd = hwnd;
            entry.frame = FrameOf(hwnd);
            entry.cloaked = IsCloaked(hwnd);
            entry.maximized = IsZoomed(hwnd) != FALSE;
            entry.minimized = IsIconic(hwnd) != FALSE;
            // 空矩形的窗口对遮挡没有贡献，也不该被描边——直接不收，省得下游到处判空。
            if (entry.frame.right > entry.frame.left &&
                entry.frame.bottom > entry.frame.top) {
                snapshot.windows.push_back(entry);
            }
        }
        hwnd = GetWindow(hwnd, GW_HWNDNEXT);
    }
    return snapshot;
}

} // namespace windowmark::win
```

- [ ] **Step 3: 加进构建**

在 `CMakeLists.txt` 的 Windows 平台源文件列表里，`src/platform/windows/WinBorderBackend.cpp` 附近加一行：

```cmake
    src/platform/windows/WinDesktopSnapshot.cpp
```

- [ ] **Step 4: 编译**

Run: `cmake --build build --config Release --target WindowMark`
Expected: 零警告通过

- [ ] **Step 5: 用现有工具交叉验证快照**

Run: `python tools\trace-borders.py`

把 trace 打出的 z 序、矩形和 `CaptureDesktop()` 的口径对一遍：DWM 边界一致、cloaked 的窗口不在列表里。

- [ ] **Step 6: 提交**

```bash
git add src/platform/windows/WinDesktopSnapshot.h src/platform/windows/WinDesktopSnapshot.cpp CMakeLists.txt
git commit -m "feat: 桌面快照（z 序 + DWM 边界 + cloaked）"
```

---

### Task 4: 从快照算出要画的所有线段

**Files:**
- Create: `src/platform/windows/WinBorderPlan.h`
- Create: `src/platform/windows/WinBorderPlan.cpp`
- Modify: `CMakeLists.txt`
- Test: `tests/core_tests.cpp`（只测纯几何那部分，见下）

**Interfaces:**
- Consumes: `DesktopSnapshot`（Task 3）、`VisibleBorderSegments`（Task 2）、`BorderModel`
- Produces:
  - `struct BorderStroke { Rect rect; unsigned color; };`
  - `std::vector<BorderStroke> PlanBorders(const DesktopSnapshot& snapshot, const std::vector<BorderModel>& models, const Settings& settings);`

- [ ] **Step 1: 写头文件**

创建 `src/platform/windows/WinBorderPlan.h`：

```cpp
#pragma once

#include "WinDesktopSnapshot.h"

#include "windowmark/core/Types.h"
#include "windowmark/core/Settings.h"

#include <vector>

namespace windowmark::win {

// 一段要画的线：一个矩形加一个 0xAARRGGBB 颜色。
struct BorderStroke {
    Rect rect;
    unsigned color{};
};

// 把「谁该有边框」和「桌面此刻长什么样」合成一串待画线段。
//
// 规则：
//   - 按 z 序从上往下扫，边扫边累积遮挡区域
//   - 非激活窗口：边框环减去**排在它上面**的所有窗口，剩下的才画
//   - 激活窗口：完整边框，不裁剪，放在最后（调用方按顺序画即可）
//   - 最大化窗口不画（置顶的除外）——它贴着工作区边缘，外面没有画边框的那几像素
[[nodiscard]] std::vector<BorderStroke> PlanBorders(const DesktopSnapshot& snapshot,
                                                    const std::vector<BorderModel>& models,
                                                    const Settings& settings);

} // namespace windowmark::win
```

- [ ] **Step 2: 写实现**

创建 `src/platform/windows/WinBorderPlan.cpp`：

```cpp
#include "WinBorderPlan.h"

#include "windowmark/core/BorderOcclusion.h"

#include <algorithm>
#include <cstdint>
#include <unordered_map>

namespace windowmark::win {
namespace {

[[nodiscard]] Rect ToCore(const RECT& r) {
    return Rect{r.left, r.top, r.right, r.bottom};
}

[[nodiscard]] HWND HwndOf(WindowId id) {
    return reinterpret_cast<HWND>(static_cast<std::uintptr_t>(id));
}

[[nodiscard]] int ReachOf(const BorderModel& model, const Settings& settings) {
    const int stroke =
        std::max(1, model.pinned ? settings.pin.width : settings.border.width);
    return std::max(0, stroke + settings.border.offset);
}

[[nodiscard]] unsigned ColorOf(const BorderModel& model, const Settings& settings,
                               unsigned accent) {
    if (model.pinned) {
        return settings.pin.color == PinSettings::kAccentColor ? accent
                                                               : settings.pin.color;
    }
    return model.active ? settings.border.activeColor : settings.border.inactiveColor;
}

} // namespace

std::vector<BorderStroke> PlanBorders(const DesktopSnapshot& snapshot,
                                      const std::vector<BorderModel>& models,
                                      const Settings& settings) {
    std::vector<BorderStroke> strokes;
    if (models.empty() || snapshot.windows.empty()) return strokes;

    // 系统主题色只在有置顶边框时才需要，读一次够了。
    unsigned accent = 0xFF3B78FF;
    for (const auto& model : models) {
        if (model.pinned && settings.pin.color == PinSettings::kAccentColor) {
            accent = SystemAccentColor();
            break;
        }
    }

    std::unordered_map<HWND, const BorderModel*> wanted;
    wanted.reserve(models.size());
    for (const auto& model : models) wanted.emplace(HwndOf(model.windowId), &model);

    std::vector<Rect> occluders;
    occluders.reserve(snapshot.windows.size());
    const BorderModel* activeModel = nullptr;
    Rect activeFrame{};

    // 从上往下扫：轮到某个窗口时，occluders 里正好是**排在它上面**的所有窗口。
    for (const auto& entry : snapshot.windows) {
        const auto it = wanted.find(entry.hwnd);
        const bool tracked = it != wanted.end();

        if (tracked && !entry.cloaked && !entry.minimized) {
            const BorderModel& model = *it->second;
            const bool skipMaximized = entry.maximized && !model.pinned;
            if (!skipMaximized) {
                const Rect frame = ToCore(entry.frame);
                const int reach = ReachOf(model, settings);
                const Rect outer{frame.left - reach, frame.top - reach,
                                 frame.right + reach, frame.bottom + reach};
                if (entry.hwnd == snapshot.foreground) {
                    // 激活窗口留到最后画，且不裁剪。
                    activeModel = &model;
                    activeFrame = frame;
                } else {
                    for (const Rect& seg :
                         VisibleBorderSegments(outer, frame, occluders)) {
                        strokes.push_back(BorderStroke{seg, ColorOf(model, settings, accent)});
                    }
                }
            }
        }

        // 不管有没有边框，这个窗口都会挡住它下面的窗口。cloaked / 最小化的不算。
        if (!entry.cloaked && !entry.minimized) occluders.push_back(ToCore(entry.frame));
    }

    if (activeModel != nullptr) {
        const int reach = ReachOf(*activeModel, settings);
        const Rect outer{activeFrame.left - reach, activeFrame.top - reach,
                         activeFrame.right + reach, activeFrame.bottom + reach};
        for (const Rect& seg : BorderRingSegments(outer, activeFrame)) {
            strokes.push_back(BorderStroke{seg, ColorOf(*activeModel, settings, accent)});
        }
    }
    return strokes;
}

} // namespace windowmark::win
```

**注意**：`SystemAccentColor()` 已经在 `WinUtil.h:44` 声明、`WinUtil.cpp:350` 实现（`WinSettingsDialog.cpp` 也在用它），本文件只需 `#include "WinUtil.h"` 即可，不用搬迁。

- [ ] **Step 3: 加进构建**

`CMakeLists.txt` 的 Windows 源文件列表加一行：

```cmake
    src/platform/windows/WinBorderPlan.cpp
```

- [ ] **Step 4: 编译**

Run: `cmake --build build --config Release --target WindowMark`
Expected: 零警告通过

- [ ] **Step 5: 提交**

```bash
git add src/platform/windows/WinBorderPlan.h src/platform/windows/WinBorderPlan.cpp CMakeLists.txt
git commit -m "feat: 从快照算出待画的边框线段"
```

---

### Task 5: Overlay 渲染器

**Files:**
- Create: `src/platform/windows/WinOverlay.h`
- Create: `src/platform/windows/WinOverlay.cpp`
- Modify: `CMakeLists.txt`

**Interfaces:**
- Consumes: `BorderStroke`（Task 4）
- Produces:
  - `class MonitorOverlay { bool Create(const RECT& monitorRect); void Render(const std::vector<BorderStroke>& strokes); void Destroy() noexcept; }`
  - `class OverlaySet { void Sync(); void Render(const std::vector<BorderStroke>& strokes); void Destroy() noexcept; }`（按显示器分发）

- [ ] **Step 1: 写头文件**

创建 `src/platform/windows/WinOverlay.h`：

```cpp
#pragma once

#include "WinBorderPlan.h"

#include <windows.h>

#include <memory>
#include <vector>

namespace windowmark::win {

// 一块显示器上的透明画布。
//
// **创建时就带 WS_EX_TOPMOST**，此后永远不执行「把普通层窗口提进 topmost 层」——
// 那正是会卡死的操作：实测存在这样的窗口，SetWindowPos 返回 TRUE、GetLastError 为 0、
// z 序和 topmost 位纹丝不动，换锚点、加 SWP_FRAMECHANGED、从别的进程调都推不动。
// 生来就在 topmost 层就不需要这个动作。
class MonitorOverlay {
public:
    ~MonitorOverlay();

    bool Create(const RECT& monitorRect);
    void Render(const std::vector<BorderStroke>& strokes);
    void Destroy() noexcept;

    [[nodiscard]] const RECT& Bounds() const { return bounds_; }

private:
    void MoveToBandTail();

    HWND hwnd_{};
    HDC dc_{};
    HBITMAP bitmap_{};
    HBITMAP oldBitmap_{};
    void* bits_{};
    RECT bounds_{};
    RECT lastDirty_{};
};

// 每块显示器一个 overlay，显示器配置变了就重建。
class OverlaySet {
public:
    void Sync();
    void Render(const std::vector<BorderStroke>& strokes);
    void Destroy() noexcept;

private:
    std::vector<std::unique_ptr<MonitorOverlay>> overlays_;
};

} // namespace windowmark::win
```

- [ ] **Step 2: 写实现**

创建 `src/platform/windows/WinOverlay.cpp`：

```cpp
#include "WinOverlay.h"

#include "AppIdentity.h"

#include <algorithm>

namespace windowmark::win {
namespace {

constexpr const wchar_t* kOverlayClass = L"WindowMark.Overlay";

bool EnsureClass() {
    static const ATOM atom = [] {
        WNDCLASSEXW wc{};
        wc.cbSize = sizeof(wc);
        wc.hInstance = GetModuleHandleW(nullptr);
        wc.lpfnWndProc = [](HWND hwnd, UINT msg, WPARAM w, LPARAM l) -> LRESULT {
            if (msg == WM_NCHITTEST) return HTTRANSPARENT;
            return DefWindowProcW(hwnd, msg, w, l);
        };
        wc.lpszClassName = kOverlayClass;
        return RegisterClassExW(&wc);
    }();
    return atom != 0;
}

// topmost 层里最靠后的那个可见窗口。overlay 插到它后面就落在两个 band 的交界：
// 比所有普通窗口高，比所有 topmost 窗口低。右键菜单、输入法候选框、任务栏、悬浮的
// 会议小窗因而都能压在边框上面。
[[nodiscard]] HWND LastTopmostWindow(HWND self) {
    HWND last = nullptr;
    HWND h = GetTopWindow(nullptr);
    for (int step = 0; step < 4096 && h != nullptr; ++step) {
        if (h != self && IsWindowVisible(h)) {
            if ((GetWindowLongPtrW(h, GWL_EXSTYLE) & WS_EX_TOPMOST) == 0) break;
            last = h;
        }
        h = GetWindow(h, GW_HWNDNEXT);
    }
    return last;
}

BOOL UnionInto(RECT& target, const RECT& add) {
    return UnionRect(&target, &target, &add);
}

} // namespace

MonitorOverlay::~MonitorOverlay() { Destroy(); }

bool MonitorOverlay::Create(const RECT& monitorRect) {
    if (!EnsureClass()) return false;
    bounds_ = monitorRect;
    const int width = std::max<LONG>(1, bounds_.right - bounds_.left);
    const int height = std::max<LONG>(1, bounds_.bottom - bounds_.top);

    hwnd_ = CreateWindowExW(
        // TRANSPARENT 让点击穿透到下面的窗口；NOACTIVATE 让它永远不抢焦点。
        WS_EX_LAYERED | WS_EX_TRANSPARENT | WS_EX_TOOLWINDOW | WS_EX_NOACTIVATE |
            WS_EX_TOPMOST,
        kOverlayClass, L"", WS_POPUP | WS_DISABLED,
        bounds_.left, bounds_.top, width, height,
        nullptr, nullptr, GetModuleHandleW(nullptr), nullptr);
    if (!hwnd_) return false;

    HDC screen = GetDC(nullptr);
    dc_ = CreateCompatibleDC(screen);
    ReleaseDC(nullptr, screen);
    if (!dc_) return false;

    BITMAPINFO info{};
    info.bmiHeader.biSize = sizeof(info.bmiHeader);
    info.bmiHeader.biWidth = width;
    info.bmiHeader.biHeight = -height;   // 负数 = 自上而下，配 ULW_ALPHA 用
    info.bmiHeader.biPlanes = 1;
    info.bmiHeader.biBitCount = 32;
    info.bmiHeader.biCompression = BI_RGB;
    bitmap_ = CreateDIBSection(dc_, &info, DIB_RGB_COLORS, &bits_, nullptr, 0);
    if (!bitmap_) return false;
    oldBitmap_ = static_cast<HBITMAP>(SelectObject(dc_, bitmap_));

    ShowWindow(hwnd_, SW_SHOWNOACTIVATE);
    MoveToBandTail();
    return true;
}

void MonitorOverlay::MoveToBandTail() {
    if (!hwnd_) return;
    // 层内换位，不是「提进层」——窗口已经在 topmost 层里，这一步不会踩到那个卡死的
    // 操作。真挪不动也无所谓：位置照样对，顶多盖住点系统 UI。
    if (HWND lastTop = LastTopmostWindow(hwnd_); lastTop != nullptr) {
        SetWindowPos(hwnd_, lastTop, 0, 0, 0, 0,
                     SWP_NOMOVE | SWP_NOSIZE | SWP_NOACTIVATE);
    }
}

void MonitorOverlay::Render(const std::vector<BorderStroke>& strokes) {
    if (!hwnd_ || !bits_) return;
    const int width = bounds_.right - bounds_.left;
    const int height = bounds_.bottom - bounds_.top;
    auto* pixels = static_cast<unsigned*>(bits_);

    // 这一帧要动的区域 = 上一帧画过的 + 这一帧要画的。只提交这块，别提交整屏。
    RECT dirty = lastDirty_;
    RECT painted{};
    bool anyPainted = false;

    // 先擦掉上一帧
    if (lastDirty_.right > lastDirty_.left) {
        const int x0 = std::max<LONG>(0, lastDirty_.left - bounds_.left);
        const int y0 = std::max<LONG>(0, lastDirty_.top - bounds_.top);
        const int x1 = std::min<LONG>(width, lastDirty_.right - bounds_.left);
        const int y1 = std::min<LONG>(height, lastDirty_.bottom - bounds_.top);
        for (int y = y0; y < y1; ++y) {
            unsigned* row = pixels + static_cast<size_t>(y) * width;
            std::fill(row + x0, row + x1, 0u);
        }
    }

    for (const BorderStroke& stroke : strokes) {
        RECT r{stroke.rect.left, stroke.rect.top, stroke.rect.right, stroke.rect.bottom};
        RECT clipped{};
        if (!IntersectRect(&clipped, &r, &bounds_)) continue;

        const int x0 = clipped.left - bounds_.left;
        const int y0 = clipped.top - bounds_.top;
        const int x1 = clipped.right - bounds_.left;
        const int y1 = clipped.bottom - bounds_.top;
        // 预乘 alpha：ULW_ALPHA 要求源位图已经乘好，否则边缘会发白。
        const unsigned a = (stroke.color >> 24) & 0xFFu;
        const unsigned r8 = ((stroke.color >> 16) & 0xFFu) * a / 255u;
        const unsigned g8 = ((stroke.color >> 8) & 0xFFu) * a / 255u;
        const unsigned b8 = (stroke.color & 0xFFu) * a / 255u;
        const unsigned premultiplied = (a << 24) | (r8 << 16) | (g8 << 8) | b8;

        for (int y = y0; y < y1; ++y) {
            unsigned* row = pixels + static_cast<size_t>(y) * width;
            std::fill(row + x0, row + x1, premultiplied);
        }

        if (anyPainted) {
            UnionInto(painted, clipped);
        } else {
            painted = clipped;
            anyPainted = true;
        }
    }

    if (anyPainted) {
        if (dirty.right > dirty.left) {
            UnionInto(dirty, painted);
        } else {
            dirty = painted;
        }
    }
    lastDirty_ = anyPainted ? painted : RECT{};

    if (dirty.right <= dirty.left || dirty.bottom <= dirty.top) return;

    POINT src{0, 0};
    SIZE size{width, height};
    POINT dst{bounds_.left, bounds_.top};
    BLENDFUNCTION blend{};
    blend.BlendOp = AC_SRC_OVER;
    blend.SourceConstantAlpha = 255;
    blend.AlphaFormat = AC_SRC_ALPHA;

    // prcDirty 的坐标以窗口客户区为准，转成相对 overlay 左上角。
    RECT dirtyLocal{dirty.left - bounds_.left, dirty.top - bounds_.top,
                    dirty.right - bounds_.left, dirty.bottom - bounds_.top};

    UPDATELAYEREDWINDOWINFO info{};
    info.cbSize = sizeof(info);
    info.pptSrc = &src;
    info.psize = &size;
    info.hdcSrc = dc_;
    info.pptDst = &dst;
    info.pblend = &blend;
    info.dwFlags = ULW_ALPHA;
    info.prcDirty = &dirtyLocal;
    UpdateLayeredWindowIndirect(hwnd_, &info);
}

void MonitorOverlay::Destroy() noexcept {
    if (dc_ && oldBitmap_) SelectObject(dc_, oldBitmap_);
    if (bitmap_) DeleteObject(bitmap_);
    if (dc_) DeleteDC(dc_);
    if (hwnd_) DestroyWindow(hwnd_);
    bitmap_ = nullptr;
    oldBitmap_ = nullptr;
    dc_ = nullptr;
    hwnd_ = nullptr;
    bits_ = nullptr;
}

void OverlaySet::Sync() {
    std::vector<RECT> monitors;
    EnumDisplayMonitors(
        nullptr, nullptr,
        [](HMONITOR monitor, HDC, LPRECT, LPARAM param) -> BOOL {
            MONITORINFO mi{};
            mi.cbSize = sizeof(mi);
            if (GetMonitorInfoW(monitor, &mi)) {
                reinterpret_cast<std::vector<RECT>*>(param)->push_back(mi.rcMonitor);
            }
            return TRUE;
        },
        reinterpret_cast<LPARAM>(&monitors));

    const bool sameLayout =
        overlays_.size() == monitors.size() &&
        std::equal(overlays_.begin(), overlays_.end(), monitors.begin(),
                   [](const std::unique_ptr<MonitorOverlay>& o, const RECT& m) {
                       return EqualRect(&o->Bounds(), &m) != FALSE;
                   });
    if (sameLayout) return;

    overlays_.clear();
    for (const RECT& m : monitors) {
        auto overlay = std::make_unique<MonitorOverlay>();
        if (overlay->Create(m)) overlays_.push_back(std::move(overlay));
    }
}

void OverlaySet::Render(const std::vector<BorderStroke>& strokes) {
    for (auto& overlay : overlays_) overlay->Render(strokes);
}

void OverlaySet::Destroy() noexcept { overlays_.clear(); }

} // namespace windowmark::win
```

- [ ] **Step 3: 加进构建并编译**

`CMakeLists.txt` 的 Windows 源文件列表加 `src/platform/windows/WinOverlay.cpp`。

Run: `cmake --build build --config Release --target WindowMark`
Expected: 零警告通过

- [ ] **Step 4: 提交**

```bash
git add src/platform/windows/WinOverlay.h src/platform/windows/WinOverlay.cpp CMakeLists.txt
git commit -m "feat: 每显示器一个 overlay 渲染器"
```

---

### Task 6: 换掉后端，接上 Coordinator

**Files:**
- Rewrite: `src/platform/windows/WinBorderBackend.h`
- Rewrite: `src/platform/windows/WinBorderBackend.cpp`

**Interfaces:**
- Consumes: `CaptureDesktop()`、`PlanBorders()`、`OverlaySet`
- Produces: 仍然实现 `IBorderBackend`，Coordinator 那一侧一行不用改

- [ ] **Step 1: 重写头文件**

`src/platform/windows/WinBorderBackend.h` 整体替换为：

```cpp
#pragma once

#include "WinOverlay.h"

#include "windowmark/core/Interfaces.h"

#include <vector>
#include <windows.h>

namespace windowmark::win {

// 边框后端：不再给每个窗口配一个 HWND 去抢 z 序，而是每块显示器一个透明画布，
// 按当前桌面快照算出该画哪几段线，一次画完。
//
// Z 序只读，不写。唯一还会动 z 序的是 overlay 自己在 topmost 层内挪到末尾，那是
// 层内换位，不是「提进层」——后者才是会静默卡死的那个操作。
class WinBorderBackend final : public IBorderBackend {
public:
    ~WinBorderBackend() override;

    bool Start(const Settings& settings) override;
    void Apply(const std::vector<BorderModel>& models) override;
    void MoveBorder(WindowId id, const Rect& frame) override;
    void UpdateSettings(const Settings& settings) override;
    void Stop() noexcept override;

private:
    void Redraw();

    Settings settings_;
    std::vector<BorderModel> models_;
    OverlaySet overlays_;
    bool started_{false};
};

} // namespace windowmark::win
```

- [ ] **Step 2: 重写实现**

`src/platform/windows/WinBorderBackend.cpp` 整体替换为：

```cpp
#include "WinBorderBackend.h"

#include "WinBorderPlan.h"
#include "WinDesktopSnapshot.h"

namespace windowmark::win {

WinBorderBackend::~WinBorderBackend() { Stop(); }

bool WinBorderBackend::Start(const Settings& settings) {
    if (started_) return true;
    settings_ = settings;
    overlays_.Sync();
    started_ = true;
    return true;
}

void WinBorderBackend::Apply(const std::vector<BorderModel>& models) {
    if (!started_) return;
    models_ = models;
    Redraw();
}

void WinBorderBackend::MoveBorder(WindowId id, const Rect& frame) {
    if (!started_) return;
    // 快速路径：只有一个窗口动了。位置从快照现取，这里只需要把模型里的 frame 跟上，
    // 免得下一次 Apply 用到旧值。
    for (auto& model : models_) {
        if (model.windowId == id) {
            model.frame = frame;
            break;
        }
    }
    Redraw();
}

void WinBorderBackend::UpdateSettings(const Settings& settings) {
    settings_ = settings;
    if (started_) Redraw();
}

void WinBorderBackend::Redraw() {
    // 显示器配置可能变了（插拔、分辨率、缩放），每帧确认一次——没变时它只做一次
    // EnumDisplayMonitors 和一次矩形比较，代价可以忽略。
    overlays_.Sync();
    const DesktopSnapshot snapshot = CaptureDesktop();
    overlays_.Render(PlanBorders(snapshot, models_, settings_));
}

void WinBorderBackend::Stop() noexcept {
    overlays_.Destroy();
    models_.clear();
    started_ = false;
}

} // namespace windowmark::win
```

**这一步删掉的东西**（都在旧文件里，整体替换后自然消失）：`BorderWindow` 类、`SyncZOrder`、`ResyncIfDrifted`、`ForceTopmost`、`LastTopmostWindow`（旧的那份，新的在 `WinOverlay.cpp`）、`LowestOwnedDialog`、`IsAdjacentTo`、`IsStuck` / `NoteZOrderResult` / `RecreateStuckBorders` / `RequestStuckRecheck`、z 序守望轮询（`StartZOrderWatch` / `StopZOrderWatch` / `WatchProc` / `ForegroundProc` / `g_watchWindow`）、`LayeredSurface`、`activeBorder_`。

- [ ] **Step 3: 编译**

Run: `cmake --build build --config Release`
Expected: 零警告通过。如果报 `PinDiag.h` 或 `BorderGeometry.h` 未使用而链接失败，检查 `CMakeLists.txt` 里是否还列着已删掉的源文件。

- [ ] **Step 4: 装上并验证**

Run: `.\reinstall.ps1`

然后：

```bash
python tools\check-maximized.py
python tools\trace-borders.py
```

Expected:
- `check-maximized.py`：「最大化却仍有边框的：0 个」
- `trace-borders.py`：不再有 `WindowMark.WindowBorder` 类的窗口（改成 overlay 了），小结无异常

**注意**：`tools/` 下几个按 `WindowMark.WindowBorder` 类名找边框的脚本（`check-border-band.py`、`check-floating-borders.py`、`check-maximized.py`、`trace-borders.py`）在这一步之后会失效——它们找的窗口已经不存在了。这是预期的：那些工具是为「每窗口一个边框」的模型写的，新模型下「边框在 z 序哪一格」这个问题本身消失了。Task 8 会处理它们。

- [ ] **Step 5: 手工回归**

按这个序列点一遍，每步看屏幕：

1. claude → chrome A → chrome B → 任务管理器 → 点回 chrome B（rc1 的复现序列）
2. 拖动一个窗口绕圈
3. 打开 Word 的查找替换、Notepad++ 的查找
4. 右键菜单、输入法候选框
5. 最大化一个窗口再还原
6. 飞书会议悬浮窗

Expected：边框始终完整且跟手；对话框、菜单、候选框、悬浮窗压在边框上面；最大化时无边框。

- [ ] **Step 6: 提交**

```bash
git add src/platform/windows/WinBorderBackend.h src/platform/windows/WinBorderBackend.cpp
git commit -m "refactor: 边框后端换成单 overlay + 遮挡裁剪"
```

---

### Task 7: 锁屏挂起

**Files:**
- Modify: `src/platform/windows/WinBorderBackend.h`
- Modify: `src/platform/windows/WinBorderBackend.cpp`
- Modify: `CMakeLists.txt`（链接 `wtsapi32`）

**Interfaces:**
- Consumes: Task 6 的 `WinBorderBackend`
- Produces: 无对外接口变化

- [ ] **Step 1: 加一个 message-only 窗口收会话通知**

在 `WinBorderBackend.h` 的 private 区加：

```cpp
    void StartSessionWatch();
    void StopSessionWatch() noexcept;
    static LRESULT CALLBACK SessionProc(HWND hwnd, UINT msg, WPARAM wParam, LPARAM lParam);

    HWND sessionWindow_{};
    bool suspended_{false};
```

在 `WinBorderBackend.cpp` 里实现：

```cpp
// 锁屏时把 overlay 收起来。
//
// 锁屏期间 GetForegroundWindow() 会返回 LockApp.exe，它会以前台窗口的身份混进快照，
// 把边框画到锁屏界面上，解锁后还要等下一个事件才恢复。收到 WTS_SESSION_LOCK 直接
// 停画，UNLOCK 时重新取一次快照。
void WinBorderBackend::StartSessionWatch() {
    if (sessionWindow_) return;
    static const ATOM sessionClass = [] {
        WNDCLASSEXW wc{};
        wc.cbSize = sizeof(wc);
        wc.hInstance = GetModuleHandleW(nullptr);
        wc.lpfnWndProc = SessionProc;
        wc.lpszClassName = L"WindowMark.SessionWatch";
        return RegisterClassExW(&wc);
    }();
    if (sessionClass == 0) return;

    sessionWindow_ = CreateWindowExW(0, L"WindowMark.SessionWatch", L"", 0, 0, 0, 0, 0,
                                     HWND_MESSAGE, nullptr, GetModuleHandleW(nullptr),
                                     this);
    if (!sessionWindow_) return;
    SetWindowLongPtrW(sessionWindow_, GWLP_USERDATA, reinterpret_cast<LONG_PTR>(this));
    WTSRegisterSessionNotification(sessionWindow_, NOTIFY_FOR_THIS_SESSION);
}

void WinBorderBackend::StopSessionWatch() noexcept {
    if (!sessionWindow_) return;
    WTSUnRegisterSessionNotification(sessionWindow_);
    DestroyWindow(sessionWindow_);
    sessionWindow_ = nullptr;
}

LRESULT CALLBACK WinBorderBackend::SessionProc(HWND hwnd, UINT msg, WPARAM wParam,
                                               LPARAM lParam) {
    if (msg == WM_WTSSESSION_CHANGE) {
        auto* self = reinterpret_cast<WinBorderBackend*>(
            GetWindowLongPtrW(hwnd, GWLP_USERDATA));
        if (self != nullptr) {
            if (wParam == WTS_SESSION_LOCK) {
                self->suspended_ = true;
                self->overlays_.Render({});   // 空列表 = 擦干净
            } else if (wParam == WTS_SESSION_UNLOCK) {
                self->suspended_ = false;
                self->Redraw();
            }
        }
        return 0;
    }
    return DefWindowProcW(hwnd, msg, wParam, lParam);
}
```

在 `Redraw()` 开头加一行：

```cpp
    if (suspended_) return;
```

在 `Start()` 里 `overlays_.Sync();` 之后加 `StartSessionWatch();`，在 `Stop()` 开头加 `StopSessionWatch();`。

在 `WinBorderBackend.cpp` 顶部加 `#include <wtsapi32.h>`。

- [ ] **Step 2: 链接 wtsapi32**

`CMakeLists.txt` 里 `WindowMark` 的 `target_link_libraries` 追加 `wtsapi32`。

- [ ] **Step 3: 编译**

Run: `cmake --build build --config Release --target WindowMark`
Expected: 零警告通过

- [ ] **Step 4: 验证**

装上后按 Win+L 锁屏，解锁，确认边框正常恢复且锁屏界面上没有边框。

- [ ] **Step 5: 提交**

```bash
git add src/platform/windows/WinBorderBackend.h src/platform/windows/WinBorderBackend.cpp CMakeLists.txt
git commit -m "feat: 锁屏时挂起 overlay"
```

---

### Task 8: 诊断工具改造 + 文档

**Files:**
- Create: `tools/check-overlay.py`
- Delete: `tools/check-border-band.py`、`tools/check-floating-borders.py`、`tools/check-dialog-borders.py`、`tools/check-owned.py`、`tools/check-active-color.py`
- Modify: `tools/check-maximized.py`、`tools/trace-borders.py`
- Modify: `CHANGELOG.md`、`README.md`、`docs/Windows开发避坑规则.md`

**Interfaces:**
- Consumes: 无
- Produces: 无

- [ ] **Step 1: 写新的 overlay 检查工具**

创建 `tools/check-overlay.py`：

```python
# Overlay 模型下还值得查的只有两件事：overlay 在不在 topmost 层的末尾，以及每块
# 显示器是不是都有一个。
#
# 旧模型那些「边框排在目标上面还是下面」「有没有孤立浮线」的检查全都没有意义了——
# 边框不再是独立窗口，也不再参与 z 序竞争。
import ctypes
from ctypes import wintypes

u = ctypes.WinDLL('user32', use_last_error=True)
u.GetForegroundWindow.restype = wintypes.HWND
u.GetTopWindow.restype = wintypes.HWND
u.GetTopWindow.argtypes = [wintypes.HWND]
u.GetWindow.restype = wintypes.HWND
u.GetWindow.argtypes = [wintypes.HWND, wintypes.UINT]
u.GetWindowLongPtrW.restype = ctypes.c_longlong
u.GetWindowLongPtrW.argtypes = [wintypes.HWND, ctypes.c_int]
try:
    u.SetProcessDpiAwarenessContext(ctypes.c_void_p(-4))
except Exception:
    pass

P = ctypes.WINFUNCTYPE(wintypes.BOOL, wintypes.HWND, wintypes.LPARAM)
WS_EX_TOPMOST = 0x8
OVERLAY_CLASS = 'WindowMark.Overlay'


def cls(h):
    b = ctypes.create_unicode_buffer(160)
    u.GetClassNameW(h, b, 160)
    return b.value


def rc(h):
    r = wintypes.RECT()
    u.GetWindowRect(h, ctypes.byref(r))
    return (r.left, r.top, r.right, r.bottom)


def enum(pred):
    out = []

    def cb(h, _):
        if pred(h):
            out.append(h)
        return True

    u.EnumWindows(P(cb), 0)
    return out


def topmost(h):
    return (u.GetWindowLongPtrW(h, -20) & WS_EX_TOPMOST) != 0


def monitors():
    out = []
    MONITORENUMPROC = ctypes.WINFUNCTYPE(wintypes.BOOL, wintypes.HANDLE, wintypes.HDC,
                                         ctypes.POINTER(wintypes.RECT), wintypes.LPARAM)

    class MONITORINFO(ctypes.Structure):
        _fields_ = [('cbSize', wintypes.DWORD), ('rcMonitor', wintypes.RECT),
                    ('rcWork', wintypes.RECT), ('dwFlags', wintypes.DWORD)]

    def cb(mon, dc, rect, param):
        mi = MONITORINFO()
        mi.cbSize = ctypes.sizeof(mi)
        if u.GetMonitorInfoW(mon, ctypes.byref(mi)):
            out.append((mi.rcMonitor.left, mi.rcMonitor.top,
                        mi.rcMonitor.right, mi.rcMonitor.bottom))
        return True

    u.EnumDisplayMonitors(None, None, MONITORENUMPROC(cb), 0)
    return out


overlays = enum(lambda h: u.IsWindowVisible(h) and cls(h) == OVERLAY_CLASS)
mons = monitors()

print('显示器 %d 块，overlay %d 个' % (len(mons), len(overlays)))
print()

problems = 0
for m in mons:
    match = [o for o in overlays if rc(o) == m]
    if match:
        print('  %s  有 overlay 0x%X  topmost=%s' % (str(m), match[0], topmost(match[0])))
        if not topmost(match[0]):
            print('     !! overlay 不在 topmost 层，会被普通窗口盖住')
            problems += 1
    else:
        print('  %s  !! 没有对应的 overlay' % str(m))
        problems += 1

print()
for o in overlays:
    below = u.GetWindow(o, 2)   # GW_HWNDNEXT
    while below and not u.IsWindowVisible(below):
        below = u.GetWindow(below, 2)
    if below and topmost(below):
        print('  !! overlay 0x%X 下面还有 topmost 窗口 %s——它盖在别人上面了'
              % (o, cls(below)[:24]))
        problems += 1

print('问题 %d 处' % problems)
```

- [ ] **Step 2: 删掉过时工具**

```bash
git rm tools/check-border-band.py tools/check-floating-borders.py tools/check-dialog-borders.py tools/check-owned.py tools/check-active-color.py
```

- [ ] **Step 3: 改 check-maximized.py**

边框不再是独立窗口，判据只能看像素。把 `grab()`（`tools/trace-borders.py:143`）和
`is_border_color()`（同文件 `:173`）连同它们依赖的 `BITMAPINFOHEADER` / `BITMAPINFO`
结构体一起复制进来，然后把 `borders = enum(...)` 那段和 `has_border()` 整体换成：

```python
def has_border(h):
    """窗口四周有没有边框色的像素。

    Overlay 模型下边框不再是独立窗口，没有「配对的边框 HWND」可找，只能看屏幕。
    四条边各采三点，任意一点命中就算有。
    """
    f = dwm(h) or rc(h)
    ring = 2      # 边框环画在窗口边界外 3px，取中间那一圈最稳
    pts = []
    for t in (0.25, 0.5, 0.75):
        pts.append((f[0] + int((f[2] - f[0]) * t), f[1] - ring))
        pts.append((f[0] + int((f[2] - f[0]) * t), f[3] + ring - 1))
        pts.append((f[0] - ring, f[1] + int((f[3] - f[1]) * t)))
        pts.append((f[2] + ring - 1, f[1] + int((f[3] - f[1]) * t)))

    x0 = min(p[0] for p in pts) - 2
    y0 = min(p[1] for p in pts) - 2
    x1 = max(p[0] for p in pts) + 3
    y1 = max(p[1] for p in pts) + 3
    px = grab(x0, y0, x1 - x0, y1 - y0)
    if px is None:
        return False
    return any(is_border_color(px(sx - x0, sy - y0)) for sx, sy in pts)
```

`enum(lambda h: ... == BORDER_CLASS)` 那一行以及 `BORDER_CLASS` 常量一并删掉。

- [ ] **Step 4: 改 trace-borders.py**

这个文件的 `edge_detail()` 本来就是像素判据，留用；要改的是配对和 z 序那部分：

1. 删掉 `def topmost_at(...)` 和 `edge_detail()` 里所有用到 `border` / `order` / `idx`
   参数的分支——「这条线上最靠前的窗口是谁」在 overlay 模型下没有意义（边框和窗口不再
   是同一层的东西），把签名简化成 `edge_detail(frame)`，只返回每条边的命中数。
2. 删掉 `trace()` 里打印「边框 z=... 期望... 位置对/错」和「z序 边框在目标前面/后面」
   的那几行，以及 `sandwich`（夹层检测）整段。
3. 保留「窗口 z=... 矩形... DWM...」和「像素 上/下/左/右」两行——那两行在新模型下
   仍然是有效信息。
4. 小结的判据改成：**前台窗口四条边必须全命中**；非前台窗口某条边缺失不再算问题
   （那正是遮挡裁剪该有的结果）。

- [ ] **Step 5: 跑一遍所有工具**

Run: `python tools\check-tools.py`
Expected: 全部通过（没有用了没定义的名字）

Run: `python tools\check-overlay.py`
Expected: 每块显示器一个 overlay，都在 topmost 层，问题 0 处

- [ ] **Step 6: 更新文档**

- `CHANGELOG.md`：在顶部加 `## v0.4.9` 一节（正式版），说明换成 overlay 模型，并把 rc1 那节的「已知问题」标注为已解决
- `README.md`：版本号从 `v0.4.9-rc1` 改为 `v0.4.9`，去掉「pre-release」和已知问题的说明
- `docs/Windows开发避坑规则.md`：把「给别人的窗口加装饰，别去精确排 z 序」那节改写——现在的结论是「根本不要用 z 序表达归属关系，改用遮挡裁剪」，并保留原来那些实测事实（`SWP_NOSENDCHANGING`、UIPI 不区分方向、`SetWindowPos` 会静默失败）作为「为什么不要走那条路」的证据

- [ ] **Step 7: 提交**

```bash
git add -A tools docs CHANGELOG.md README.md
git commit -m "docs: overlay 模型的诊断工具与文档"
```

---

## 验收记录

Task 1 的性能关卡结果填在这里：

| 提交方式 | 中位数 | p90 | p99 | 最慢 | 一帧内 |
|---|---|---|---|---|---|
| 脏矩形 | 待填 | 待填 | 待填 | 待填 | 待填 |
| 整屏 | 待填 | 待填 | 待填 | 待填 | 待填 |
| **基线（现有实现）** | **3.00 ms** | — | **15.70 ms** | — | **100%** |

结论：待填（通过 / 需要换渲染策略）

## 回滚

每个任务都是独立提交，出问题 `git revert` 单个提交即可。Task 6 是不可逆点——它整体
替换了边框后端；在那之前的任务都只是新增文件，不影响现有行为。真要整体回退，回到
`v0.4.9-rc1` 这个 tag。
