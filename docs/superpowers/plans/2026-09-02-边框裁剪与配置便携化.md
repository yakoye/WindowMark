# 边框裁剪与配置便携化 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 边框不再跨屏溢出到隔壁显示器；配置文件支持便携模式和用户指定路径。

**Architecture:** 两块工作各自把「纯计算」和「系统调用」拆开。边框裁剪的几何判断、配置位置的优先级判断都提取成 core 的纯函数，进现有 `windowmark_core_tests`；平台层只负责 `MonitorFromRect`、读写注册表、文件存在性这些系统调用。这样两处的核心语义都有单测兜底，平台层剩下的部分靠实机验证。

**Tech Stack:** C++20 / Win32 / CMake / MSVC（`/W4 /WX`，警告即错误）

## Global Constraints

- 设计依据：`docs/superpowers/specs/2026-09-02-边框裁剪与配置便携化-design.md`
- `reach = max(0, border.width + border.offset)`，当前默认配置为 `4 + (-1) = 3`
- 逐边夹取条件为 `0 <= gap <= reach`，**下界不可省略**，否则手动横跨两屏的窗口会被误裁
- 配置查找优先级固定为：exe 同目录 → 注册表 `ConfigPath` → `%LOCALAPPDATA%`
- 注册表位置 `HKCU\Software\WindowMark`，值名 `ConfigPath`，类型 `REG_SZ`，存含文件名的完整路径；值不存在或为空串等同「未指定」
- 编译零警告，`python tools/check-escapes.py` 必须通过
- 含反斜杠的内容一律用 Write 工具或 Write 出来的 python 脚本改，不走 shell heredoc/sed
- 构建：`cmake --build build --config Release --parallel`
- 跑单测：`build\Release\windowmark_core_tests.exe`
- 装机验证：`powershell -NoProfile -ExecutionPolicy Bypass -File .\reinstall.ps1`

---

### Task 1: 边框裁剪的几何计算（core，可单测）

**Files:**
- Create: `include/windowmark/core/BorderGeometry.h`
- Create: `src/core/BorderGeometry.cpp`
- Modify: `CMakeLists.txt`（加入 core 源文件列表）
- Test: `tests/core_tests.cpp`

**Interfaces:**
- Consumes: `windowmark::Rect`（`include/windowmark/core/Types.h`）
- Produces: `windowmark::ClampBorderToMonitor(const Rect& frame, const Rect& outer, const Rect& monitor, int reach) -> Rect`

- [ ] **Step 1: 先写会失败的测试**

在 `tests/core_tests.cpp` 顶部的 include 区加一行：

```cpp
#include "windowmark/core/BorderGeometry.h"
```

在匿名 namespace 结尾（`} // namespace` 之前）加入：

```cpp
void TestBorderClamping() {
    const Rect monitor{0, 0, 1920, 1080};
    const int reach = 3;

    // 最大化：DWM 扩展边界正好等于工作区。左上右三边贴合监视器，
    // 底边离监视器底部还有任务栏那 60px，不该夹。
    {
        const Rect frame{0, 0, 1920, 1020};
        const Rect outer{-3, -3, 1923, 1023};
        const Rect got = ClampBorderToMonitor(frame, outer, monitor, reach);
        CHECK(got.left == 0);
        CHECK(got.top == 0);
        CHECK(got.right == 1920);
        CHECK(got.bottom == 1023);
    }

    // 半屏吸附：左上贴合，右边在屏幕中间，那条边要保持正常外扩。
    {
        const Rect frame{0, 0, 960, 1020};
        const Rect outer{-3, -3, 963, 1023};
        const Rect got = ClampBorderToMonitor(frame, outer, monitor, reach);
        CHECK(got.left == 0);
        CHECK(got.top == 0);
        CHECK(got.right == 963);
    }

    // 手动横跨两屏：窗口右边落在隔壁屏中间，相对本监视器 gap 为负。
    // 这是下界 0 存在的唯一理由——没有它这条边会被误裁。
    {
        const Rect frame{1500, 100, 2500, 800};
        const Rect outer{1497, 97, 2503, 803};
        const Rect got = ClampBorderToMonitor(frame, outer, monitor, reach);
        CHECK(got.left == 1497);
        CHECK(got.right == 2503);
    }

    // 普通窗口四边都离监视器很远，原样返回。
    {
        const Rect frame{699, 162, 1846, 884};
        const Rect outer{696, 159, 1849, 887};
        const Rect got = ClampBorderToMonitor(frame, outer, monitor, reach);
        CHECK(got.left == 696);
        CHECK(got.top == 159);
        CHECK(got.right == 1849);
        CHECK(got.bottom == 887);
    }

    // reach 为 0 时边框不外扩，也就没有可夹的东西。
    {
        const Rect frame{0, 0, 1920, 1080};
        const Rect outer{0, 0, 1920, 1080};
        const Rect got = ClampBorderToMonitor(frame, outer, monitor, 0);
        CHECK(got.left == 0);
        CHECK(got.right == 1920);
    }
}
```

在 `main()` 里 `TestHotkeyParsing();` 之后加一行：

```cpp
    TestBorderClamping();
```

- [ ] **Step 2: 跑一次确认它编译不过**

```bash
cmake --build build --config Release --parallel
```

预期：报错找不到 `windowmark/core/BorderGeometry.h`。

- [ ] **Step 3: 写头文件**

`include/windowmark/core/BorderGeometry.h`：

```cpp
#pragma once

#include "windowmark/core/Types.h"

namespace windowmark {

// 把边框的外矩形夹回窗口所在的监视器。
//
// 边框画在窗口外 reach 像素。窗口最大化时它的可见边界正好等于工作区，那圈外扩就落到
// 了屏幕外面——单屏被裁掉看不见，双屏则越过分界线画到隔壁屏上。半屏吸附同理。
//
// 只夹「窗口自己已经贴到监视器边界」的那几条边，判据是 gap 落在 [0, reach] 之内。
// 下界 0 不能省：手动横跨两块屏的窗口，边缘落在本监视器外面（gap 为负），没有下界
// 那条边会被误裁，那是回归而不是修复。
[[nodiscard]] Rect ClampBorderToMonitor(const Rect& frame, const Rect& outer,
                                        const Rect& monitor, int reach);

} // namespace windowmark
```

- [ ] **Step 4: 写实现**

`src/core/BorderGeometry.cpp`：

```cpp
#include "windowmark/core/BorderGeometry.h"

#include <algorithm>

namespace windowmark {

Rect ClampBorderToMonitor(const Rect& frame, const Rect& outer, const Rect& monitor,
                          int reach) {
    Rect result = outer;
    if (reach <= 0) return result;

    // 窗口这条边到监视器对应边的间隙。贴合时为 0，窗口跑到监视器外面时为负。
    const auto touches = [reach](int gap) { return gap >= 0 && gap <= reach; };

    if (touches(frame.left - monitor.left)) {
        result.left = std::max(result.left, monitor.left);
    }
    if (touches(frame.top - monitor.top)) {
        result.top = std::max(result.top, monitor.top);
    }
    if (touches(monitor.right - frame.right)) {
        result.right = std::min(result.right, monitor.right);
    }
    if (touches(monitor.bottom - frame.bottom)) {
        result.bottom = std::min(result.bottom, monitor.bottom);
    }
    return result;
}

} // namespace windowmark
```

- [ ] **Step 5: 加进构建**

`CMakeLists.txt` 的 `add_library(windowmark_core STATIC` 源文件列表里，按字母序插入：

```cmake
    src/core/BorderGeometry.cpp
```

（放在 `src/core/Coordinator.cpp` 之前）

- [ ] **Step 6: 编译并跑测试**

```bash
cmake --build build --config Release --parallel
build\Release\windowmark_core_tests.exe
```

预期：编译零警告，输出 `WindowMark core tests passed.`

- [ ] **Step 7: 提交**

```bash
git add include/windowmark/core/BorderGeometry.h src/core/BorderGeometry.cpp CMakeLists.txt tests/core_tests.cpp
git commit -m "边框裁剪的几何计算，含跨屏窗口不被误裁的用例"
```

---

### Task 2: 把裁剪接进边框渲染

**Files:**
- Modify: `src/platform/windows/WinBorderBackend.cpp`（`BorderWindow::OuterRect()`）

**Interfaces:**
- Consumes: Task 1 的 `ClampBorderToMonitor`；`windowmark::win::ToCoreRect` / `ToWinRect`（`WinUtil.h`，签名 `Rect ToCoreRect(const RECT&)` / `RECT ToWinRect(const Rect&)`）
- Produces: 无新接口，`OuterRect()` 返回值语义不变

- [ ] **Step 1: 加 include**

`src/platform/windows/WinBorderBackend.cpp` 的 include 区，在 `#include "WinUtil.h"` 之后加：

```cpp
#include "windowmark/core/BorderGeometry.h"
```

- [ ] **Step 2: 改 OuterRect**

把现有的：

```cpp
    [[nodiscard]] RECT OuterRect() const {
        const int reach = Reach();
        return RECT{
            model_.frame.left - reach,
            model_.frame.top - reach,
            model_.frame.right + reach,
            model_.frame.bottom + reach,
        };
    }
```

替换为：

```cpp
    [[nodiscard]] RECT OuterRect() const {
        const int reach = Reach();
        Rect outer{
            model_.frame.left - reach,
            model_.frame.top - reach,
            model_.frame.right + reach,
            model_.frame.bottom + reach,
        };

        // 夹回窗口所在的监视器，别让那圈外扩落到隔壁屏幕上。用 MonitorFromRect 而不是
        // MonitorFromWindow：这里只有几何，不依赖那个 HWND 此刻是否还有效。
        const RECT frameRect = ToWinRect(model_.frame);
        if (HMONITOR monitor = MonitorFromRect(&frameRect, MONITOR_DEFAULTTONEAREST)) {
            MONITORINFO info{};
            info.cbSize = sizeof(info);
            if (GetMonitorInfoW(monitor, &info)) {
                outer = ClampBorderToMonitor(model_.frame, outer, ToCoreRect(info.rcMonitor),
                                             reach);
            }
        }
        return ToWinRect(outer);
    }
```

- [ ] **Step 3: 编译**

```bash
cmake --build build --config Release --parallel
```

预期：零错误零警告。

- [ ] **Step 4: 装机**

```bash
powershell -NoProfile -ExecutionPolicy Bypass -File .\reinstall.ps1
```

- [ ] **Step 5: 实机验证四种窗口状态**

本轮 scratchpad 里已有两个现成脚本，直接复用：

- `check-maximized.ps1` — 逐个列出被描边的窗口，对照「窗口所在屏 vs 边框所在屏」和四边外扩量，不符合就标红。判据是 `窗口所在屏 == 边框所在屏` 且外扩量为 `3,3,3,3`。**改完之后最大化窗口的外扩量会变成 `0,0,3,3` 一类（贴边的那几条被夹到 0），所以这个脚本的判据要相应放宽**：贴边的边允许为 0，其余仍应为 3。
- `probe-maximized-geom.ps1` — 把一个窗口在还原/最大化之间切换并打印 `GetWindowRect`、DWM 边界、监视器与工作区的关系，用来复核前面那张实测表。

两个脚本都在 `C:\Users\color\AppData\Local\Temp\claude\C--Users-color-Downloads-WindowMark\f9de0f3a-bab5-4eb3-bc48-7fbf6432aca8\scratchpad\` 下（session 相关，换 session 需按上述判据重写）。

按下面四种状态逐项测量「边框矩形是否超出所在监视器」：

| 状态 | 期望 |
|---|---|
| 普通还原 | 外扩 `3,3,3,3`，不触及监视器边界 |
| 最大化 | 左/上/右不超出监视器；底边允许落进任务栏区域 |
| 半屏吸附（Win+←） | 左/上不超出；右边仍外扩 3px |
| 手动横跨两屏 | 与改动前逐像素一致 |

单屏环境下「横跨两屏」这一项无法测，记录为「受单测 `TestBorderClamping` 覆盖」。

- [ ] **Step 6: 提交**

```bash
git add src/platform/windows/WinBorderBackend.cpp
git commit -m "边框夹回窗口所在监视器，修掉双屏下最大化时溢出到隔壁屏"
```

---

### Task 3: 配置位置的优先级判断（core，可单测）

**Files:**
- Create: `include/windowmark/core/ConfigLocation.h`
- Create: `src/core/ConfigLocation.cpp`
- Modify: `CMakeLists.txt`
- Test: `tests/core_tests.cpp`

**Interfaces:**
- Produces:
  - `enum class windowmark::ConfigSource { Portable, Configured, Fallback }`
  - `struct windowmark::ConfigLocationInputs { std::filesystem::path portable; bool portableExists; std::filesystem::path configured; bool configuredUsable; std::filesystem::path fallback; }`
  - `struct windowmark::ConfigLocation { std::filesystem::path path; ConfigSource source; bool configuredUnavailable; }`
  - `windowmark::ResolveConfigLocation(const ConfigLocationInputs&) -> ConfigLocation`

- [ ] **Step 1: 先写会失败的测试**

`tests/core_tests.cpp` 的 include 区加：

```cpp
#include "windowmark/core/ConfigLocation.h"
```

匿名 namespace 结尾前加入：

```cpp
void TestConfigLocationPriority() {
    ConfigLocationInputs in;
    in.portable = "D:/portable/settings.conf";
    in.configured = "E:/custom/settings.conf";
    in.fallback = "C:/local/settings.conf";

    // 三个都没命中时用默认位置
    {
        in.portableExists = false;
        in.configuredUsable = false;
        const ConfigLocation got = ResolveConfigLocation(in);
        CHECK(got.source == ConfigSource::Fallback);
        CHECK(got.path == in.fallback);
    }

    // 只有注册表指定了且可用
    {
        in.portableExists = false;
        in.configuredUsable = true;
        const ConfigLocation got = ResolveConfigLocation(in);
        CHECK(got.source == ConfigSource::Configured);
        CHECK(got.path == in.configured);
    }

    // 便携优先于注册表指定。注册表跟着机器走，exe 旁边的 conf 跟着程序走；
    // U 盘插到别人电脑上不该去读那台机器的本地路径。
    {
        in.portableExists = true;
        in.configuredUsable = true;
        const ConfigLocation got = ResolveConfigLocation(in);
        CHECK(got.source == ConfigSource::Portable);
        CHECK(got.path == in.portable);
    }

    // 注册表里有值但那个位置已经用不了（U 盘拔了、目录被删）时回落默认，
    // 并且要能把「发生了回落」这件事报出去——静默回落等于骗用户。
    {
        in.portableExists = false;
        in.configuredUsable = false;
        in.configured = "E:/gone/settings.conf";
        const ConfigLocation got = ResolveConfigLocation(in);
        CHECK(got.source == ConfigSource::Fallback);
        CHECK(got.configuredUnavailable);
    }

    // 便携赢了不算「回落」，不该报警——用户没指望注册表那份生效。
    {
        in.portableExists = true;
        in.configuredUsable = false;
        in.configured = "E:/gone/settings.conf";
        const ConfigLocation got = ResolveConfigLocation(in);
        CHECK(got.source == ConfigSource::Portable);
        CHECK(!got.configuredUnavailable);
    }

    // 压根没指定过，也不该报警
    {
        ConfigLocationInputs plain;
        plain.fallback = "C:/local/settings.conf";
        const ConfigLocation got = ResolveConfigLocation(plain);
        CHECK(got.source == ConfigSource::Fallback);
        CHECK(!got.configuredUnavailable);
    }

    // 防御：标记说存在但路径是空的，不能因此选中一个空路径
    {
        ConfigLocationInputs empty;
        empty.portableExists = true;
        empty.configuredUsable = true;
        empty.fallback = "C:/local/settings.conf";
        const ConfigLocation got = ResolveConfigLocation(empty);
        CHECK(got.source == ConfigSource::Fallback);
        CHECK(got.path == empty.fallback);
    }
}
```

`main()` 里 `TestBorderClamping();` 之后加：

```cpp
    TestConfigLocationPriority();
```

- [ ] **Step 2: 跑一次确认编译不过**

```bash
cmake --build build --config Release --parallel
```

预期：找不到 `windowmark/core/ConfigLocation.h`。

- [ ] **Step 3: 写头文件**

`include/windowmark/core/ConfigLocation.h`：

```cpp
#pragma once

#include <filesystem>

namespace windowmark {

// settings.conf 最终用的是哪一份。
enum class ConfigSource {
    Portable,    // exe 同目录，跟着程序走
    Configured,  // 注册表 ConfigPath 指定，跟着机器走
    Fallback,    // %LOCALAPPDATA%\WindowMark
};

// 三个候选位置，外加各自是否可用。存在性和可写性由平台层判断后填进来，这里只做选择，
// 所以这段优先级逻辑可以脱离文件系统被测试。
struct ConfigLocationInputs {
    std::filesystem::path portable;
    bool portableExists{false};
    std::filesystem::path configured;
    bool configuredUsable{false};
    std::filesystem::path fallback;
};

struct ConfigLocation {
    std::filesystem::path path;
    ConfigSource source{ConfigSource::Fallback};
    // 注册表里明明指定了位置，但那个位置已经用不了（U 盘拔了、目录被删、变成只读），
    // 于是回落到了默认位置。调用方必须把这件事告诉用户：静默回落会让人以为自己的
    // 配置还在原处，直到发现改动全丢了才反应过来。
    bool configuredUnavailable{false};
};

// 便携排在显式指定之前，是刻意的：注册表跟着机器走，exe 旁边的 conf 跟着程序走。
// 代价是可能反直觉——用户指定了路径却因为 exe 旁边有个 conf 而没生效，所以设置界面
// 必须显示当前实际生效的那一个。
[[nodiscard]] ConfigLocation ResolveConfigLocation(const ConfigLocationInputs& inputs);

} // namespace windowmark
```

- [ ] **Step 4: 写实现**

`src/core/ConfigLocation.cpp`：

```cpp
#include "windowmark/core/ConfigLocation.h"

namespace windowmark {

ConfigLocation ResolveConfigLocation(const ConfigLocationInputs& inputs) {
    if (inputs.portableExists && !inputs.portable.empty()) {
        return ConfigLocation{inputs.portable, ConfigSource::Portable, false};
    }
    if (inputs.configuredUsable && !inputs.configured.empty()) {
        return ConfigLocation{inputs.configured, ConfigSource::Configured, false};
    }
    // 走到这里而注册表里又确实有值，说明那个位置没法用了，得让调用方去提示。
    const bool lost = !inputs.configured.empty();
    return ConfigLocation{inputs.fallback, ConfigSource::Fallback, lost};
}

} // namespace windowmark
```

- [ ] **Step 5: 加进构建**

`CMakeLists.txt` 的 core 源文件列表里，在 `src/core/Coordinator.cpp` 之后插入：

```cmake
    src/core/ConfigLocation.cpp
```

- [ ] **Step 6: 编译并跑测试**

```bash
cmake --build build --config Release --parallel
build\Release\windowmark_core_tests.exe
```

预期：零警告，`WindowMark core tests passed.`

- [ ] **Step 7: 提交**

```bash
git add include/windowmark/core/ConfigLocation.h src/core/ConfigLocation.cpp CMakeLists.txt tests/core_tests.cpp
git commit -m "配置文件位置的优先级判断，便携优先于注册表指定"
```

---

### Task 4: 平台层三层查找接线

**Files:**
- Modify: `src/shared/AppIdentity.h`（新增注册表常量）
- Modify: `src/platform/windows/WinUtil.h` / `WinUtil.cpp`（读写注册表、可写性检测、便携候选路径）
- Modify: `src/platform/windows/WinMain.cpp`（替换现有的 `dataRoot / L"settings.conf"`）

**Interfaces:**
- Consumes: Task 3 的 `ResolveConfigLocation` / `ConfigLocationInputs` / `ConfigLocation` / `ConfigSource`
- Produces:
  - `windowmark::win::PortableConfigPath() -> std::filesystem::path`（exe 同目录的 `settings.conf`）
  - `windowmark::win::ReadConfiguredConfigPath() -> std::filesystem::path`（读注册表，未设返回空）
  - `windowmark::win::WriteConfiguredConfigPath(const std::filesystem::path&) -> bool`（空路径表示清除）
  - `windowmark::win::IsDirectoryWritable(const std::filesystem::path&) -> bool`
  - `windowmark::win::CurrentConfigLocation() -> ConfigLocation`

- [ ] **Step 1: 加注册表常量**

`src/shared/AppIdentity.h` 在 `kUninstallKeyPath` 之后加入：

```cpp
// 产品自己的注册表根。到 v0.4.4 为止这里只有系统的 Run / Uninstall 键，配置文件位置
// 是第一个需要 WindowMark 自己存一条设置的地方——它不可能存在配置文件里，因为得先
// 找到配置文件才能读它。
inline constexpr wchar_t kProductKeyPath[] = L"Software\\WindowMark";
inline constexpr wchar_t kConfigPathValue[] = L"ConfigPath";
```

- [ ] **Step 2: WinUtil.h 加声明**

在 `std::filesystem::path LocalDataRoot();` 之后加入：

```cpp
// 配置文件的三个候选位置。存在性与可写性在这里判断，选哪一个交给 core 的
// ResolveConfigLocation，那段逻辑因此可以脱离文件系统被单测覆盖。
[[nodiscard]] std::filesystem::path PortableConfigPath();
[[nodiscard]] std::filesystem::path ReadConfiguredConfigPath();
bool WriteConfiguredConfigPath(const std::filesystem::path& path);
[[nodiscard]] bool IsDirectoryWritable(const std::filesystem::path& directory);
[[nodiscard]] ConfigLocation CurrentConfigLocation();
```

`WinUtil.h` 的 include 区加：

```cpp
#include "windowmark/core/ConfigLocation.h"
```

- [ ] **Step 3: WinUtil.cpp 写实现**

在 `LocalDataRoot()` 之后加入：

```cpp
std::filesystem::path PortableConfigPath() {
    const auto exe = InstalledExePath();
    if (exe.empty()) return {};
    return exe.parent_path() / L"settings.conf";
}

std::filesystem::path ReadConfiguredConfigPath() {
    HKEY key = nullptr;
    if (RegOpenKeyExW(HKEY_CURRENT_USER, app::kProductKeyPath, 0, KEY_QUERY_VALUE, &key) !=
        ERROR_SUCCESS) {
        return {};
    }
    DWORD type = 0;
    DWORD bytes = 0;
    LSTATUS status =
        RegQueryValueExW(key, app::kConfigPathValue, nullptr, &type, nullptr, &bytes);
    if (status != ERROR_SUCCESS || (type != REG_SZ && type != REG_EXPAND_SZ) ||
        bytes < sizeof(wchar_t)) {
        RegCloseKey(key);
        return {};
    }
    std::vector<wchar_t> buffer(bytes / sizeof(wchar_t) + 1, L'\0');
    status = RegQueryValueExW(key, app::kConfigPathValue, nullptr, &type,
                              reinterpret_cast<BYTE*>(buffer.data()), &bytes);
    RegCloseKey(key);
    if (status != ERROR_SUCCESS || buffer[0] == L'\0') return {};
    return std::filesystem::path(buffer.data());
}

bool WriteConfiguredConfigPath(const std::filesystem::path& path) {
    HKEY key = nullptr;
    if (RegCreateKeyExW(HKEY_CURRENT_USER, app::kProductKeyPath, 0, nullptr, 0,
                        KEY_SET_VALUE, nullptr, &key, nullptr) != ERROR_SUCCESS) {
        return false;
    }
    LSTATUS status = ERROR_SUCCESS;
    if (path.empty()) {
        status = RegDeleteValueW(key, app::kConfigPathValue);
        if (status == ERROR_FILE_NOT_FOUND) status = ERROR_SUCCESS;
    } else {
        const std::wstring text = path.wstring();
        status = RegSetValueExW(key, app::kConfigPathValue, 0, REG_SZ,
                                reinterpret_cast<const BYTE*>(text.c_str()),
                                static_cast<DWORD>((text.size() + 1) * sizeof(wchar_t)));
    }
    RegCloseKey(key);
    return status == ERROR_SUCCESS;
}

bool IsDirectoryWritable(const std::filesystem::path& directory) {
    if (directory.empty()) return false;
    std::error_code ec;
    if (!std::filesystem::is_directory(directory, ec)) return false;
    // 真写一次再删掉。只看权限位在网络盘和只读介质上会给出错误答案。
    const auto probe = directory / L"windowmark-write-probe.tmp";
    HANDLE file = CreateFileW(probe.c_str(), GENERIC_WRITE, 0, nullptr, CREATE_ALWAYS,
                              FILE_ATTRIBUTE_TEMPORARY | FILE_FLAG_DELETE_ON_CLOSE, nullptr);
    if (file == INVALID_HANDLE_VALUE) return false;
    CloseHandle(file);
    return true;
}

ConfigLocation CurrentConfigLocation() {
    ConfigLocationInputs inputs;
    std::error_code ec;

    inputs.portable = PortableConfigPath();
    inputs.portableExists =
        !inputs.portable.empty() && std::filesystem::exists(inputs.portable, ec);

    inputs.configured = ReadConfiguredConfigPath();
    inputs.configuredUsable =
        !inputs.configured.empty() && IsDirectoryWritable(inputs.configured.parent_path());

    const auto root = LocalDataRoot();
    inputs.fallback = root.empty() ? std::filesystem::path{} : root / L"settings.conf";

    return ResolveConfigLocation(inputs);
}
```

`WinUtil.cpp` 的 include 区确认含有 `<vector>`；没有就加上。

- [ ] **Step 4: WinMain 改用它**

把 `src/platform/windows/WinMain.cpp` 里的：

```cpp
    const std::filesystem::path dataRoot = windowmark::win::LocalDataRoot();
    if (dataRoot.empty()) {
        LogAutoStartPhase(autoStartLaunch, L"data_path_failed", 4);
        MessageBoxW(nullptr, L"无法确定本地配置目录。", L"WindowMark", MB_OK | MB_ICONERROR);
        return 4;
    }
    const auto settingsPath = dataRoot / L"settings.conf";
```

替换为：

```cpp
    // 三层查找：exe 同目录 -> 注册表 ConfigPath -> %LOCALAPPDATA%。
    const windowmark::ConfigLocation configLocation = windowmark::win::CurrentConfigLocation();
    if (configLocation.path.empty()) {
        LogAutoStartPhase(autoStartLaunch, L"data_path_failed", 4);
        MessageBoxW(nullptr, L"无法确定配置文件位置。", L"WindowMark", MB_OK | MB_ICONERROR);
        return 4;
    }
    const auto settingsPath = configLocation.path;

    // 指定过位置但那里已经没了（U 盘拔了、目录被删、变成只读）。这时候用的是默认位置的
    // 配置，用户看到的会是一套「回到默认」的设置——不说清楚他会以为软件把设置弄丢了。
    // 开机自启动时也照样弹：这是罕见事件，而后果是整套设置都不是他要的那份。
    if (configLocation.configuredUnavailable) {
        MessageBoxW(nullptr,
                    L"指定的配置文件位置当前不可用，已改用默认位置。\n\n"
                    L"常见原因：U 盘没插、目录被删、或那个位置变成了只读。\n"
                    L"接好之后在托盘菜单的「配置文件...」里重新指定即可。",
                    L"WindowMark", MB_OK | MB_ICONWARNING);
    }
```

若 `dataRoot` 在后文还有别处引用，保留其定义，只改 `settingsPath` 的来源。

- [ ] **Step 5: 编译**

```bash
cmake --build build --config Release --parallel
python tools/check-escapes.py
```

预期：零警告，转义检查通过。

- [ ] **Step 6: 三层各验证一次**

装机后按下表逐条验证，每次改完重启 WindowMark，用 `WindowMarkInspect.exe` 或直接看哪个文件的修改时间在变：

| 步骤 | 操作 | 期望 |
|---|---|---|
| 1 | 不做任何事 | 用 `%LOCALAPPDATA%\WindowMark\settings.conf` |
| 2 | 把该文件拷到 `%LOCALAPPDATA%\Programs\WindowMark\` | 改动保存到 exe 同目录那份 |
| 3 | 删掉 exe 同目录那份，注册表写 `HKCU\Software\WindowMark\ConfigPath` 指向 `D:\wmtest\settings.conf` | 用 `D:\wmtest\settings.conf` |
| 4 | 把 exe 同目录那份放回来 | 用 exe 同目录那份（便携优先） |

- [ ] **Step 7: 提交**

```bash
git add src/shared/AppIdentity.h src/platform/windows/WinUtil.h src/platform/windows/WinUtil.cpp src/platform/windows/WinMain.cpp
git commit -m "配置文件三层查找接线：exe 同目录 / 注册表指定 / 默认位置"
```

---

### Task 5: 「配置文件...」对话框与位置切换

**Files:**
- Create: `src/platform/windows/WinConfigPathDialog.h`
- Create: `src/platform/windows/WinConfigPathDialog.cpp`
- Modify: `CMakeLists.txt`（加入 WindowMark 目标的源文件）
- Modify: `src/platform/windows/WinControlWindow.h` / `.cpp`（菜单项与命令派发）
- Modify: `src/platform/windows/WinMain.cpp`（处理器接线）

**Interfaces:**
- Consumes: Task 4 的 `CurrentConfigLocation` / `WriteConfiguredConfigPath` / `IsDirectoryWritable` / `PortableConfigPath`；`windowmark::Settings::Save`
- Produces: `windowmark::win::WinConfigPathDialog::ShowModal(HWND owner, const std::filesystem::path& current, ConfigSource currentSource, std::filesystem::path& chosen) -> bool`

- [ ] **Step 1: 菜单项与命令 id**

`WinControlWindow.h` 在 `kBorderExcludeCommand = 1015;` 之后加：

```cpp
    static constexpr UINT kConfigPathCommand = 1016;
```

`Handlers` 结构在 `onAbout` 之前加：

```cpp
        // 配置文件位置。跟开机启动一样是程序级设置，所以放在托盘菜单而不是某个功能的
        // 设置页里。
        std::function<void()> onConfigPath;
```

- [ ] **Step 2: 菜单里加一项**

`WinControlWindow.cpp` 的 `ShowMenu()` 中，把：

```cpp
    AppendMenuW(menu, MF_STRING, kAboutCommand, L"关于");
```

改为：

```cpp
    AppendMenuW(menu, MF_STRING, kConfigPathCommand, L"配置文件...");
    AppendMenuW(menu, MF_STRING, kAboutCommand, L"关于");
```

命令派发处，在 `case kAboutCommand:` 之前加：

```cpp
        case kConfigPathCommand:     handler = &handlers_.onConfigPath; break;
```

启动自检的 `wired` 数组里加一行：

```cpp
        {L"onConfigPath", static_cast<bool>(handlers_.onConfigPath)},
```

- [ ] **Step 3: 写对话框头文件**

`src/platform/windows/WinConfigPathDialog.h`：

```cpp
#pragma once

#include "windowmark/core/ConfigLocation.h"

#include <windows.h>

#include <filesystem>

namespace windowmark::win {

// 选配置文件放在哪。三个选项正好对应三层查找，不引入第四种概念。
//
// 对话框顶部显示当前实际生效的那一份——因为便携排在显式指定之前，用户可能指定了路径
// 却因为 exe 旁边有个 conf 而没生效，不显示出来没人能想明白。
class WinConfigPathDialog {
public:
    // 用户按下确定时返回 true，chosen 写入选中的完整路径（含文件名）。
    // 选「默认位置」时 chosen 为默认路径，调用方据此清空注册表值。
    static bool ShowModal(HWND owner, const std::filesystem::path& current,
                          ConfigSource currentSource, std::filesystem::path& chosen);
};

} // namespace windowmark::win
```

- [ ] **Step 4: 写对话框实现**

`src/platform/windows/WinConfigPathDialog.cpp`。整体照 `WinRenameDialog.cpp`（204 行）的结构：
`CreateUiFont(dpi)` → `Prompt` 类 → `Run(owner)` 注册窗口类、建窗口、`EnableWindow(owner, FALSE)`、
自己的 `GetMessageW` + `IsDialogMessageW` 模态循环 → `S(value)` 做 DPI 缩放 → `Add(...)` 建控件。
把那个文件的 `Run()` / `S()` / `Add()` / `WndProc()` 整体抄过来改类名即可，下面只给不同的部分。

控件 id 与布局（96 DPI 基准，实际坐标一律经 `S()`）：

```cpp
constexpr int kCurrentId  = 2001;  // STATIC  当前生效
constexpr int kDefaultId  = 2002;  // RADIO   默认位置
constexpr int kDefPathId  = 2003;  // STATIC  灰色路径
constexpr int kPortableId = 2004;  // RADIO   程序目录
constexpr int kPortPathId = 2005;  // STATIC  灰色路径
constexpr int kCustomId   = 2006;  // RADIO   自定义
constexpr int kPathEditId = 2007;  // EDIT
constexpr int kBrowseId   = 2008;  // BUTTON  浏览...
constexpr int kOkId       = 2009;
constexpr int kCancelId   = 2010;

// 窗口 520 x 262
void Build() {
    Add(L"STATIC", currentText_.c_str(), WS_CHILD | WS_VISIBLE,
        16, 12, 488, 36, kCurrentId);

    Add(L"BUTTON", L"默认位置", WS_CHILD | WS_VISIBLE | WS_GROUP | BS_AUTORADIOBUTTON,
        16, 60, 488, 24, kDefaultId);
    Add(L"STATIC", fallback_.wstring().c_str(), WS_CHILD | WS_VISIBLE,
        40, 84, 464, 18, kDefPathId);

    Add(L"BUTTON", L"程序目录（便携，跟着程序走）",
        WS_CHILD | WS_VISIBLE | BS_AUTORADIOBUTTON, 16, 108, 488, 24, kPortableId);
    Add(L"STATIC", portable_.wstring().c_str(), WS_CHILD | WS_VISIBLE,
        40, 132, 464, 18, kPortPathId);

    Add(L"BUTTON", L"自定义", WS_CHILD | WS_VISIBLE | BS_AUTORADIOBUTTON,
        16, 156, 488, 24, kCustomId);
    Add(L"EDIT", customDir_.c_str(),
        WS_CHILD | WS_VISIBLE | WS_BORDER | WS_TABSTOP | ES_AUTOHSCROLL,
        40, 182, 364, 24, kPathEditId);
    Add(L"BUTTON", L"浏览...", WS_CHILD | WS_VISIBLE | WS_TABSTOP,
        412, 182, 92, 24, kBrowseId);

    Add(L"BUTTON", L"确定", WS_CHILD | WS_VISIBLE | WS_TABSTOP | BS_DEFPUSHBUTTON,
        332, 218, 84, 28, kOkId);
    Add(L"BUTTON", L"取消", WS_CHILD | WS_VISIBLE | WS_TABSTOP,
        420, 218, 84, 28, kCancelId);

    CheckRadioButton(hwnd_, kDefaultId, kCustomId, InitialRadioId());
    SyncEnabled();
}
```

顶部那行文字与初始选中项：

```cpp
[[nodiscard]] const wchar_t* SourceLabel(ConfigSource source) {
    switch (source) {
    case ConfigSource::Portable:   return L"程序目录";
    case ConfigSource::Configured: return L"自定义";
    case ConfigSource::Fallback:   break;
    }
    return L"默认位置";
}

// 便携排在显式指定之前，用户可能指定了路径却因为 exe 旁边有个 conf 而没生效。
// 不把实际生效的那一份显示出来，没人能想明白为什么。
currentText_ = std::wstring(L"当前生效：") + current_.wstring() +
               L"\n（" + SourceLabel(currentSource_) + L"）";

[[nodiscard]] int InitialRadioId() const {
    switch (currentSource_) {
    case ConfigSource::Portable:   return kPortableId;
    case ConfigSource::Configured: return kCustomId;
    case ConfigSource::Fallback:   break;
    }
    return kDefaultId;
}
```

选中项 → 完整路径。用户在「自定义」里选的是**目录**，文件名由我们补，省得他自己拼：

```cpp
[[nodiscard]] std::filesystem::path SelectedPath() const {
    if (IsDlgButtonChecked(hwnd_, kPortableId) == BST_CHECKED) return portable_;
    if (IsDlgButtonChecked(hwnd_, kCustomId) == BST_CHECKED) {
        wchar_t buffer[MAX_PATH * 2]{};
        GetDlgItemTextW(hwnd_, kPathEditId, buffer, static_cast<int>(std::size(buffer)));
        std::wstring text(buffer);
        Trim(text);
        if (text.empty()) return {};
        return std::filesystem::path(text) / L"settings.conf";
    }
    return fallback_;
}

void SyncEnabled() {
    const BOOL custom = IsDlgButtonChecked(hwnd_, kCustomId) == BST_CHECKED;
    EnableWindow(GetDlgItem(hwnd_, kPathEditId), custom);
    EnableWindow(GetDlgItem(hwnd_, kBrowseId), custom);
}
```

消息处理。可写性校验做两次：**切换单选时**给即时反馈（只提示不拦，否则用户没法把焦点挪走），
**按确定时**真正拦住——自定义路径是手打进去的，改完并不触发单选事件，只靠前者会漏。

```cpp
LRESULT Handle(UINT msg, WPARAM wParam, LPARAM lParam) {
    switch (msg) {
    case WM_COMMAND:
        switch (LOWORD(wParam)) {
        case kDefaultId:
        case kPortableId:
        case kCustomId:
            if (HIWORD(wParam) == BN_CLICKED) {
                SyncEnabled();
                WarnIfUnwritable(SelectedPath());
            }
            return 0;

        case kBrowseId: {
            const auto dir = BrowseForFolder(hwnd_);
            if (!dir.empty()) {
                SetDlgItemTextW(hwnd_, kPathEditId, dir.c_str());
                WarnIfUnwritable(dir / L"settings.conf");
            }
            return 0;
        }

        case kOkId: {
            const auto candidate = SelectedPath();
            if (candidate.empty()) {
                MessageBoxW(hwnd_, L"请先填一个目录。", L"WindowMark",
                            MB_OK | MB_ICONINFORMATION);
                return 0;
            }
            // 这里不能只提示——真让它过去，配置就搬进一个存不住的地方了。
            if (!IsDirectoryWritable(candidate.parent_path())) {
                WarnIfUnwritable(candidate);
                return 0;
            }
            result_ = candidate;
            accepted_ = true;
            DestroyWindow(hwnd_);
            return 0;
        }

        case kCancelId:
            DestroyWindow(hwnd_);
            return 0;
        default:
            break;
        }
        break;

    case WM_CLOSE:
        DestroyWindow(hwnd_);
        return 0;
    case WM_DESTROY:
        PostQuitMessage(0);
        return 0;
    default:
        break;
    }
    return DefWindowProcW(hwnd_, msg, wParam, lParam);
}

void WarnIfUnwritable(const std::filesystem::path& candidate) const {
    if (candidate.empty()) return;
    if (IsDirectoryWritable(candidate.parent_path())) return;
    // 当场说，别等用户按下确定、配置都搬过去了才发现存不进去。
    MessageBoxW(hwnd_,
                L"这个位置写不进去，换一个吧。\n\n"
                L"装在 C:\\Program Files 这类目录时会遇到这种情况。",
                L"WindowMark", MB_OK | MB_ICONWARNING);
}
```

「浏览...」用 `IFileDialog` 选目录（`#include <shobjidl.h>`）：

```cpp
[[nodiscard]] std::filesystem::path BrowseForFolder(HWND owner) {
    IFileDialog* dialog = nullptr;
    if (FAILED(CoCreateInstance(CLSID_FileOpenDialog, nullptr, CLSCTX_INPROC_SERVER,
                                IID_PPV_ARGS(&dialog)))) {
        return {};
    }
    DWORD options = 0;
    dialog->GetOptions(&options);
    dialog->SetOptions(options | FOS_PICKFOLDERS | FOS_FORCEFILESYSTEM);

    std::filesystem::path chosen;
    if (SUCCEEDED(dialog->Show(owner))) {
        IShellItem* item = nullptr;
        if (SUCCEEDED(dialog->GetResult(&item))) {
            PWSTR text = nullptr;
            if (SUCCEEDED(item->GetDisplayName(SIGDN_FILESYSPATH, &text))) {
                chosen = text;
                CoTaskMemFree(text);
            }
            item->Release();
        }
    }
    dialog->Release();
    return chosen;
}
```

先确认 `WinMain.cpp` 已经调过 `CoInitializeEx`（托盘和快捷方式那部分应该已经需要它）。没有的话在 `ShowModal` 里自己配一对 `CoInitializeEx` / `CoUninitialize`，别依赖别处的初始化。

`Trim` 直接抄 `WinRenameDialog.cpp:178` 那个。

- [ ] **Step 5: 加进构建**

`CMakeLists.txt` 的 `add_executable(WindowMark WIN32` 源文件列表里加：

```cmake
        src/platform/windows/WinConfigPathDialog.cpp
```

- [ ] **Step 6: WinMain 接线，含搬数据与 .disabled 改名**

在 `handlers.onAbout` 之前加入：

```cpp
    handlers.onConfigPath = [&]() {
        exclusive([&] {
            const auto before = windowmark::win::CurrentConfigLocation();
            std::filesystem::path chosen;
            if (!windowmark::win::WinConfigPathDialog::ShowModal(
                    control.NativeHandle(), before.path, before.source, chosen)) {
                return;
            }
            if (chosen == before.path) return;

            // 先把现有配置搬过去，否则用户改完设置会发现配置"没了"。
            if (!windowmark::Settings::Save(chosen, coordinator.CurrentSettings())) {
                MessageBoxW(control.NativeHandle(),
                            L"写入新位置失败，配置位置未改变。", L"WindowMark",
                            MB_OK | MB_ICONWARNING);
                return;
            }

            const auto portable = windowmark::win::PortableConfigPath();
            const bool choosingPortable = !portable.empty() && chosen == portable;

            // 从便携切走时，exe 旁边那份必须改名。它在查找顺序里排第一，留着的话下次
            // 启动又会把它选回来，用户改的设置等于没生效。改名而不是删除：那是用户的
            // 数据，不是我们的。
            if (before.source == windowmark::ConfigSource::Portable && !choosingPortable) {
                std::error_code ec;
                std::filesystem::rename(portable, portable.wstring() + L".disabled", ec);
            }

            // 注册表只在选「自定义」时才写。选默认位置或程序目录都要清掉它，
            // 否则下次启动第 2 层会把旧值又捡回来。
            const auto fallbackRoot = windowmark::win::LocalDataRoot();
            const auto fallback =
                fallbackRoot.empty() ? std::filesystem::path{} : fallbackRoot / L"settings.conf";
            const bool custom = !choosingPortable && chosen != fallback;
            windowmark::win::WriteConfiguredConfigPath(custom ? chosen
                                                              : std::filesystem::path{});

            MessageBoxW(control.NativeHandle(),
                        L"配置位置已更改，重启 WindowMark 后生效。", L"WindowMark",
                        MB_OK | MB_ICONINFORMATION);
        });
    };
```

- [ ] **Step 7: 编译**

```bash
cmake --build build --config Release --parallel
python tools/check-escapes.py
```

预期：零警告。

- [ ] **Step 8: 装机并验证切换**

```bash
powershell -NoProfile -ExecutionPolicy Bypass -File .\reinstall.ps1
```

逐条验证：

| 场景 | 期望 |
|---|---|
| 打开对话框 | 「当前生效」与实际读取的文件一致 |
| 默认位置 → 自定义 `D:\wmtest\` | 配置内容搬过去；注册表写入；重启后用新位置 |
| 自定义 → 程序目录 | exe 同目录出现 conf；注册表被清空；重启后用便携 |
| 程序目录 → 默认位置 | exe 同目录那份变成 `settings.conf.disabled`；重启后用默认位置 |
| 自定义指向只读目录 | 选中时当场拒绝，不写入任何东西 |

- [ ] **Step 9: 提交**

```bash
git add src/platform/windows/WinConfigPathDialog.h src/platform/windows/WinConfigPathDialog.cpp src/platform/windows/WinControlWindow.h src/platform/windows/WinControlWindow.cpp src/platform/windows/WinMain.cpp CMakeLists.txt
git commit -m "托盘加「配置文件...」，切换位置时搬数据并把便携那份改名停用"
```

---

### Task 6: 文档、版本、发布

**Files:**
- Modify: `src/shared/AppIdentity.h`（版本号 → `0.4.5`）
- Modify: `CMakeLists.txt`（`project(... VERSION 0.4.5 ...)`）
- Modify: `CHANGELOG.md` / `README.md` / `VALIDATION.md`
- Modify: `docs/Windows开发避坑规则.md`

- [ ] **Step 1: 升版本**

`AppIdentity.h` 的 `kProductVersion` 与 `CMakeLists.txt` 的 `project(...)` 同步改成 `0.4.5`。两处不一致会在 configure 阶段直接失败，这是既有的防线。

- [ ] **Step 2: CHANGELOG 加 v0.4.5 段**

写清楚两件事，并把实测数据带上：最大化时 DWM 扩展边界等于工作区、边框外扩 3px 因此越界；配置三层查找及便携优先于注册表的理由。

- [ ] **Step 3: README 写清便携模式的真实限制**

必须包含这三条，否则用户会误以为改个设置就解决了还原工具的问题：

- 还原工具场景要把**整个 WindowMark 目录**搬到非 C 盘，只挪配置文件没用（程序默认就装在 C 盘）
- 搬走后若 C 盘安装版还在，两个实例会因单例互斥量冲突，第二个直接退出
- 便携模式下 `border.excluded_apps` / `selection.disabled_apps` 记录的是**其他应用**的绝对路径，换机器会静默失效；开机自启动的 Run 键同理，盘符一变就指不到

- [ ] **Step 4: VALIDATION 记录本轮实测**

最大化几何数据、四种窗口状态的边框验证、三层查找的四步验证、切换位置的五个场景。

- [ ] **Step 5: 避坑规则加一条**

```markdown
### 边框类效果要考虑「窗口边缘就是屏幕边缘」

- 向外扩 N 像素画边框，在窗口最大化或半屏吸附时，那 N 像素会落到屏幕外。单屏被裁掉
  看不见，双屏则越过分界线画到隔壁屏上——用户看到的是「边框跑到另一个显示器了」。
- 修法是逐边夹回监视器，判据 `0 <= gap <= reach`。**下界不能省**：手动横跨两块屏的
  窗口边缘落在本监视器外面（gap 为负），没有下界那条边会被误裁。
```

- [ ] **Step 6: 全量构建 + 单测 + 装机**

```bash
cmake --build build --config Release --parallel
build\Release\windowmark_core_tests.exe
powershell -NoProfile -ExecutionPolicy Bypass -File .\reinstall.ps1
```

- [ ] **Step 7: 打包、提交、发布**

按 v0.4.4 的既有流程：`dist/WindowMark-v0.4.5-win64/` 装齐四个 exe 加 README/CHANGELOG 加 tools，压包，校验哈希，提交打 tag 推送，`gh release create`。

---

## 风险与回滚

| 风险 | 影响 | 应对 |
|---|---|---|
| 夹取误伤横跨两屏的窗口 | 边框被裁掉一条边 | 单测 `TestBorderClamping` 专门覆盖；下界 `0 <=` 是防线 |
| 便携优先反直觉 | 用户指定路径不生效且不知为何 | 对话框顶部显示当前实际生效的那一份 |
| 切换位置丢配置 | 用户设置全没 | 先写新位置成功才改注册表；旧文件只改名不删除 |
| exe 目录只读 | 便携模式存不进去 | 选中时当场试写拒绝；启动时不可用则回落默认并提示 |

单个任务出问题：`git revert` 该任务的提交即可，任务之间没有跨任务的数据迁移。
