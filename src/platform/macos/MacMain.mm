#import <Cocoa/Cocoa.h>

int main(int argc, const char* argv[]) {
    @autoreleasepool {
        (void)argc;
        (void)argv;
        NSLog(@"WindowMark macOS backend scaffold is present, but window overlays are not enabled in v0.2.0.");
    }
    return 0;
}
