# 验证状态 — v0.3.7

rc3 是第一个在 Windows 上真实构建并运行过的版本。rc1/rc2 只在 Linux 容器里验证过平台无关的 Core。

## 验证环境

- Windows 11 Home China 10.0.26200
- Visual Studio 18 Professional / MSVC 19.51.36252 / Windows SDK 10.0.26100.0
- 显示：双屏，主屏 2560x1440 位于 (0,0)，副屏 1920x1080 位于 (-1920,360)；**两块都是 125% 缩放（120 DPI）**
  - 书签的验证（rc3–v0.2.0 阶段）是在单屏环境下做的；边框的层级审计覆盖了两块屏上的窗口
- CMake + Visual Studio 生成器

125% 缩放这一点很关键：rc2 的渲染缺陷只在非 100% 缩放下才会暴露。

## 已验证通过

### 构建

- `build.bat` 完整通过，Release 与 Debug 两个配置均零错误、零警告（`/W4 /permissive-`）。
- 五个可执行目标全部产出：`WindowMark.exe`、`WindowMarkInspect.exe`、`WindowMarkSetup.exe`、`WindowMarkUninstall.exe`、`windowmark_core_tests.exe`。

### 测试

- `windowmark_core_tests` 在 **Release 和 Debug 下都通过**（此前 Release 下会崩溃，原因见 CHANGELOG）。
- `ctest -C Release`：1/1 通过。

### v0.3.7 托盘菜单与开机启动

- 托盘菜单结构通过窗口消息打开并读取 `HMENU`，核对标题、两个功能子菜单、总开关、开机启动、关于和退出项。
- 开机启动开关经过“启用 → 注册表值存在 → 菜单勾选 → 禁用 → 注册表值删除”的往返验证；启用时会清除 Windows 的 `StartupApproved\\Run` 否决位。
- 设置对话框高度由字段表计算；两个页面均核对最低输入控件与按钮行之间保留 47px 间距。

### 层级与激活标记

- 只有**前台窗口**显示书签条：实测 12 个已创建的覆盖层中恒定只有 1 个 `IsWindowVisible`，切换前台窗口时随之转移。rc2 中后台窗口的书签浮在前台窗口之上的问题不再出现。
- 所有书签在**根部**（长出来的那条边）切成直角，与窗口无缝相接。激活书签只靠几何区分：更长，且越过窗口边缘（其余停在边缘处），视觉上压在窗口之上、其余嵌在窗口之下。没有描边、投影或透明度差异，每个书签的填充完全一致。侧边布局已截图逐像素核对根部直角与越界伸出。
- 底部行（宿主最大化时）平时半高贴窗口底边、悬停朝上展开，根部为底边。**此项未能截图验证**，原因见下方未验证清单。

### 运行时渲染

用两个以上同应用窗口（Explorer、浏览器）实际截图核对：

- 书签条出现在宿主窗口左侧外缘，位置与 `LayoutEngine` 的计算一致，无偏移。
- 每个书签是**彩虹色圆角标签**，背景**完全透明**，圆角抗锯齿；rc2 的黑色矩形底已消除。
- 折叠宽度 30px，标签文字居中且完整（中文 2 字 / 英文 3 字符，无省略号残缺）。
- 同一应用的每个窗口都得到同一组书签，数量与窗口数一致。

### 资源占用

实测数据（12 逻辑核，7 个 Explorer 窗口同组，即 7 个覆盖层同时存在）：

| 指标 | 数值 |
|---|---|
| 空闲 CPU | 0.022%（12 秒采样） |
| 工作集 / 私有内存 | 51.7 MB / 36.5 MB |
| 句柄 / 线程 | 285 / 4 |
| GDI 对象 / USER 对象 | 18 / 30 |
| 持续拖动窗口时 CPU | 单核 14.8%（约占整机 1.2%），5 次运行中位数 |

拖动那一项是**合成压力**：脚本以 100Hz 移动窗口并同时以 100Hz 移动光标，节流后约 30Hz 触发刷新，比真人拖动更密集。优化前为单核约 20%（且未叠加光标流量）。

内存 51.7 MB 中的大头是 Direct2D/DirectWrite 的固定开销，与书签数量无关。GDI 18 / USER 30 在长时间运行后保持稳定，无泄漏迹象。

### 窗口边框（v0.3.0 新增）

- Core 单元测试覆盖：默认关闭、开启后覆盖全部顶级窗口（含无书签的单窗口应用）、活动标记随焦点转移、书签关闭时边框仍更新、免节流移动路径走 `MoveBorder` 而非重建、关闭后清空。
- 实机截图核对窗口角部：圆角完整跟随系统圆角，抗锯齿正常。
- 设置界面：托盘「窗口边框 → 边框设置...」打开独立窗口，字段与分组核对无误；颜色以 `#RRGGBB[AA]` 输入，非法值会明确报错。

#### 层级审计（任务管理器问题修复后）

脚本枚举桌面上每一个可见顶级窗口，按中心点匹配它的边框窗口，比较两者在 z 序中的位置：

| 检查 | 结果 |
|---|---|
| 静态检查：边框是否都在各自窗口之上 | 9 个窗口，**0 个错误** |
| 逐个把窗口抬到最上层打乱 z 序、等 3 秒 | **0 个错误**（全部自愈）|
| 边框与目标之间是否夹住了**可见**窗口 | **0 处** |

第三项是修复本身带来的新风险点：跳过隐藏窗口意味着边框会越过它们，必须确认没有连可见窗口一起越过。夹在中间的（最多一处夹了 9 个）全部是隐藏窗口，不产生像素。

任务管理器的右上角与右下角已单独放大截图核对：描边连续、圆角跟随系统、抗锯齿正常。

#### 边框颜色与接缝

用户反馈「两处边框颜色看起来不一样，不是纯色吗」。沿一条边逐像素采样 11 个位置，四条边各采 5 个位置：**同一条边框在所有位置的像素值完全相同**，所以颜色本身没有位置差异，看起来不同是背景对比度造成的。

但对比中发现一个真问题——`offset` 决定描边有没有盖住 Windows 给窗口自己画的那 1px 边框：

| 配置 | Reach | 上边由外向内（Chrome 系 / 资源管理器）|
|---|---|---|
| `w3/off0` | 3px | 3 行纯色 + **`4F5255` / `646765` 灰缝** ❌ |
| `w3/off-1` | 2px | 3 行纯色，无缝 ✅ |
| **`w4/off-1`（新默认）** | 3px | **4 行纯色，无缝** ✅ |
| `w5/off-1` | 4px | 5 行纯色，无缝 ✅ |

`offset=0` 时描边停在窗口外一格，Windows 自己那条边框就露出来夹在中间，8 倍放大截图下清晰可见。`offset=-1` 压住它就没有缝。新默认取 `width=4, offset=-1`：既无缝，又比 3px 更容易在杂乱背景上分辨。

排除过的猜测：不是抗锯齿（路径 `inset = stroke/2`，描边覆盖整数像素边界）；不是渲染目标 DPI（已钉 96）；不是颜色 alpha（`#RRGGBB` 解析出 alpha=255）；不是与窗口内容混合（黑底/白底测试窗口下那一行的值完全相同）。

> 过程中一度在 WinForms 测试窗口上量到 `offset=-1` 激活时最内侧一行是 `6A75BA`，据此把默认改成了 `offset=0`。后续在 Chrome 与资源管理器上做四配置受控对比时**复现不出来**，判断当时是配置改动与进程重启之间的竞态读到了旧状态。默认已改回 `offset=-1`。

#### 系统 UI 排除（输入法闪烁）

现象：打字时屏幕上一闪一闪出现一整圈边框，切换输入法的浮出也带边框。

根因用 `list_tracked.bat` 定位：`TextInputHost` 有一个 **2560×1440 全屏尺寸**的
`Windows.UI.Core.CoreWindow`（标题「Windows 输入体验」），平时被 DWM **cloaked**，所以
静止时被 cloaked 检查挡住看不出问题；打字或弹出切换浮出时它 uncloak，立刻通过跟踪过滤，
被当成一个全屏普通窗口画上边框，收起来又 cloak 回去——cloak/uncloak 循环就是闪烁。

修复后做了对照验证：自建两个窗口，**除类名外条件完全相同**（可见、未 cloaked、无属主、
非 tool window、同尺寸同样式），这正是输入法窗口弹出时的状态：

| 测试窗口类名 | 期望 | 实际 |
|---|---|---|
| `Windows.UI.Core.CoreWindow` | 无边框 | 无边框 ✅ |
| `WindowMarkTestOrdinaryClass` | 有边框 | 有边框 ✅ |

排除生效，且没有误伤普通窗口。

后来发现只排这一个类不够——候选条和切换浮出是**另外两个窗口**。用 `list_tracked.bat` 盯 30 秒（记录「实际被画了边框」的窗口）抓到：

| 类名 | 进程 | 尺寸 | 是什么 |
|---|---|---|---|
| `Shell_InputSwitchTopLevelWindow` | explorer | 480x410 | 输入法切换浮出，标题就叫 Input Flyout |
| `ApplicationFrameWindow` | explorer | 751x90 | 输入法候选条 |

候选条那个**不能整类排除**：`ApplicationFrameWindow` 是所有 UWP 应用的宿主框架。改为按持有进程区分——真应用的框架属于 `ApplicationFrameHost.exe`，shell 自用的属于 `explorer.exe`。实测四个实例分得干净：

| 窗口 | 进程 | pid | 结果 |
|---|---|---|---|
| 计算器 | ApplicationFrameHost | 22828 | 保留边框 ✅ |
| 设置 | ApplicationFrameHost | 22828 | 保留边框 ✅ |
| （无标题）×2 | explorer | 8344 | 排除 ✅ |

真实输入法候选框的肉眼确认仍需人工。

#### 置顶（always-on-top）窗口

自建一个置顶、无属主、非 tool-window 的测试窗口，走完整的开→关→移动→再开循环：

| 状态 | 目标 TOPMOST | 边框 TOPMOST | 层级 |
|---|---|---|---|
| 初始（置顶）| True | True | 差 1 ✅ |
| 取消置顶 0.3s | False | False | 差 1 ✅ |
| 取消置顶 2.3s | False | False | 差 1 ✅ |
| 移动之后 | False | False | 差 1 ✅ |
| 重新置顶 | True | True | 差 1 ✅ |

修复前「取消置顶」那三行是 `边框 TOPMOST=True`、浮在目标上方 22–32 位——也就是压在一堆无关窗口之上，且永不恢复。

#### 与 tacky-borders 的实测对比

同一个资源管理器窗口、同样的 80 次程序化拖动、每次移动后 16ms 采样边框与窗口边缘的偏差。**两者分别单独运行**（同时运行会互相干扰：那种条件下测得 50/80 对 47/80，是假性持平）。

| 指标 | WindowMark | tacky-borders |
|---|---|---|
| 完美跟随 | **80/80** | 80/80 |
| 平均滞后 | **0px** | 0px |
| 私有内存（12 个边框） | **54.9MB** | 73MB |
| 工作集 | 122MB | **67.5MB** |
| 线程数 | **4** | 32 |

跟手已与之持平；私有内存与线程数更优；工作集偏高的原因与后续计划见 ROADMAP.md。

#### 定位跟手问题的过程（三次证伪）

值得记录，因为每一步都推翻了当时最像样的猜测：

1. **不是跨进程调用**。给 `WS_CHILD` 判定和 DWM 边界查询加缓存后，完美跟随从 62% 变成 60% —— 在噪声范围内。
2. **不是主线程被书签抢占**。完全关闭书签后测得 46/80，与开启时**完全一致**。这条同时否定了「每功能一线程」能解决问题的假设。
3. **不是事件丢失**。自行安装 hook 计数，80 次移动送达 79 个 `EVENT_OBJECT_LOCATIONCHANGE`。
4. **真正原因是一个边框由四个条带窗口组成** —— 每个位置事件要移动四个窗口而不是一个。改回单窗口后立即变成 80/80。

四条带原本是为省内存引入的；换成「单窗口 + 全进程共享一张位图」后，内存与跟手可以兼得。

### 拖动流畅度（v0.3.6）— 换了尺子之后重测

v0.3.5 那套「统计卡住次数」的指标信噪比太低，得出的几个结论后来都被推翻了。v0.3.6 换成
把链路拆成两段分别量：

- **A 段**：注入的坐标 → 系统光标实际位置。反映系统输入管线。
- **B 段**：系统光标 → 被拖窗口的位置。反映被拖的程序自己有没有被卡住。

两段都用**轨迹时间对齐**求延迟（把一条轨迹整体平移，找残差方差最小的位移量），这样不受
抓取点、拖动速度、恒定偏移影响。另外报**跟随率** = 窗口走过的路程 ÷ 光标走过的路程，
1.0 表示完全跟上，0.4 表示漏掉六成——窗口跟不上时不是「整体晚一点」，而是整段整段漏掉
往返，纯时间平移对不齐这种失真，两个指标要一起看。

拖动为 8 字形，x 半幅 780px、y 半幅 400px，每秒 2 个来回（峰值约 9800px/s），
**4 个条件轮转交错跑 5 轮取中位数**：

| 条件 | 延迟中位 | 跟随率中位 |
|---|---|---|
| 书签+边框 | 32ms | 0.971 |
| 只开书签 | 24ms | 0.969 |
| 只开边框 | 30ms | 0.958 |
| WindowMark 未运行 | 10ms | 0.983 |

修复前「书签+边框」是 **174.7ms / 跟随率 0.024**。A 段在所有条件下恒为 **0ms、跟随率
1.000**——系统输入管线始终正常，问题全在 B 段。

### 层级审计（v0.3.6）

去掉 owner 之后层级要全部靠自己维护，所以每次改动都用脚本逐窗口核对，不靠肉眼：

- **书签条**：来回切换两个同组 Excel 窗口 6 次，每次核对「无主」且「位于前台窗口上层」，
  7/7 通过（z=9 对前台 z=65）。
- **边框**：桌面上 7 个可见边框逐个匹配宿主（用 DWM 边界四边相差 6px 以内认定），核对
  是否在宿主上方、中间有没有夹别的可见窗口、有没有被误设置顶。终态 7/7 符合预期——
  6 个在宿主上方，前台窗口那一个紧贴宿主下方（这是前台锁下能达到的最好结果，只丢压在
  窗框上的 1px），无一被提升到置顶层。

审计过程中真的抓到两个回归，都不是肉眼能可靠发现的：一个边框跑到 z=14 且置顶位为真，
另一个卡在无关窗口下面。

#### 这一轮踩过的坑

- **交错是必须的。** 四个条件各自连着跑完时，机器上的慢漂移会整段落到某个条件头上：同一
  份代码的「只开书签」在两次运行里测出 17.3ms 和 66.7ms。轮转交错 + 取中位数之后稳定在
  24ms 附近。
- **极值不能当结果。** 交错之后连「WindowMark 未运行」都出现过 200ms / 跟随率 0.00 的
  单次样本。极值是环境抖动，只有中位数可用。
- **让用户手动拖那一版失败了。** 合格门槛设成「光标与窗口轨迹相关性 > 0.9」，可相关性低
  恰恰就是卡顿本身——窗口漏掉整段往返时相位对不上，相关性自然掉到 0.1~0.7。结果把五次
  有效数据全判成了废数据。症状不能拿来当有效性判据。

### 拖动流畅度（v0.3.5，指标已废弃）

用 SendInput 真按住 Excel 标题栏、匀速拖 80 步，每步之后采样窗口位置，统计「注入了移动但窗口一步没动」的次数。这条路径走的是窗口自己的模态拖动循环，和手动拖是同一条。每个配置 7 轮：

| 配置 | 卡住中位 | 均值 | 散布 | 平均滞后 | WM 占用 CPU |
|---|---|---|---|---|---|
| WindowMark 未运行 | 1/80 | 0.4 | 0~1 | 0.06px | — |
| 只边框 | 1/80 | 0.9 | 0~3 | 0.09px | 391ms |
| 只书签 | 1/80 | 0.9 | 0~2 | 0.11px | 359ms |
| 书签+边框 | 3/80 | 2.7 | 1~5 | 0.50px | 516ms |

**单开任一功能都贴着基线，两个都开才明显变差**，而且这一条在四轮独立测量里每次都成立。

#### 这里踩过的坑

- 第一次测的时候目标窗口铺满副屏、处于吸附状态，四轮里三轮**窗口纹丝不动**，被记成「卡到极致」——连基线都是 80/80 卡住，一眼假。现在每轮先强制还原成 1200×800 不吸附，并且加了「一步都没动过 = 数据无效」的判定。
- **三轮不够。** 三轮时测出「只书签 1.3、只边框 0.3」，据此认定书签开销是边框的四倍；七轮之后两者都是 0.9，差异消失。散布必须打出来，只看均值会把噪声当成效果。
- 有一轮「书签+边框」出现 69/80，与之前的 1~5 完全不符；重跑没有复现，判定为一次性系统抖动，不作为数据。

#### 分步耗时（拖动进行中，60 次采样，微秒）

| 步骤 | 中位数 | 平均 |
|---|---|---|
| 资格判定（5 个跨进程调用）| 224 | 275 |
| 进程路径（缓存未命中）| 200 | 221 |
| DWM 扩展边界 | 152 | 209 |
| 窗口标题 | 6 | 7 |
| 其余四项合计 | ~12 | ~13 |
| **一次 BuildWindowInfo** | | **~725** |

一次 80 步拖动收到 **274 个顶级窗口 LOCATIONCHANGE**（约 114/秒，Excel 自己在连续上报）。274 × 1.2ms ≈ 330ms，与实测的 328ms CPU 吻合——**开销是按事件付的**。

顺带纠正一个当时的猜测：以为 `RefreshAll` 全量枚举很贵。实测桌面 816 个窗口里只有 43 个会走完整查询，**全量枚举只要 1.2ms**，而且拖动期间触发它的事件是 **0 次**。

### 书签文字始终可见（v0.2.0）

字号此前固定 13px，而标签高度由设置决定。用 DirectWrite 实测：13px 需要 **17.3px 行高**，超过默认折叠标签的 16px。超出布局矩形的文字不会被 `D2D1_DRAW_TEXT_OPTIONS_CLIP` 截断，而是**整块消失**——这就是「某个书签变成纯色块」的成因。现改为按每个标签自身高度推导字号（激活项与其余项高度不同，故逐标签决定）。

穷举验证（独立小程序，真实 DirectWrite 度量）：

| 维度 | 覆盖范围 |
|---|---|
| 标签高度 | 10、12、14、16、17、20、23、28、34、40、80 px（10px 是 `thickness=20` 时能产生的最小值）|
| 折叠宽度 | 24、30、44、60、100 px |
| 标签文本 | 拉丁、中文、日文、西里尔、emoji、纯数字符号、单字符，共 10 种 |

**550 种组合，0 例出现空文本、超宽或超高。** 字号映射：10px→7、12px→8、14px→9、16px→11、20px 及以上→13。

### 折叠标签的自适应截断（rc4 新增）

用独立小程序以相同字体（Segoe UI Variable Text 13px）和相同可用宽度调用 DirectWrite 实测：

| 标题构成 | 固定取 3 字 | 自适应后 |
|---|---|---|
| 5 个汉字（此前显示为空白的那个） | 3 字 = **39.0px，超出** | 2 字 = 26.0px，放得下 |
| 3 个西文字母 | 30.4px，放得下 | 不变 |
| 2 西文 + 1 汉字 | 27.0px，放得下 | 不变 |
| 2 汉字 + 1 数字 | 33.2px，放得下 | 不变 |

44px 折叠标签去掉两侧内边距后可用 35.2px。三个汉字恰好越界，而含西文的标题都在范围内——这解释了为何只有特定标签变空白。溢出文本配合 `D2D1_DRAW_TEXT_OPTIONS_CLIP` 是整块消失而非部分显示。修复后仅越界的标签被截短，其余不受影响。

### 设置界面与重命名（rc4 新增）

通过向托盘控制窗口投递菜单命令打开对话框后，逐控件核对：

- 设置对话框正常打开，77 个控件、全部分组标题与标签正确；19 个数值输入框**全部已填充**（无空值），下拉框选中项与 `placement=bottom` 一致，两个复选框状态与配置一致。
- **取消**：对话框关闭，WindowMark 进程存活。
- **确定**：对话框关闭，配置文件被重写且各项值正确落盘，**进程 PID 不变** —— 确认是热应用而非重启。
- 模态对话框嵌套在主消息循环内而不会连带退出主程序（早期版本在 `WM_DESTROY` 里 `PostQuitMessage` 会导致托盘程序一起退出，已修）。

### 安装 / 卸载

全部通过 `WindowMarkSetup.exe` / `WindowMarkUninstall.exe` 实测：

| 场景 | 结果 |
|---|---|
| 全新静默安装 `/S` | 程序目录、卸载注册项、开始菜单快捷方式、进程启动，全部就位 |
| **在程序运行中重装** | 连续 5 次全部成功，退出码 0，每次约 2 秒，PID 正确更替，**全程无报错** |
| 静默卸载 `/S` | 进程停止、程序目录删除、卸载注册项删除、快捷方式删除、自启项删除 |
| 卸载后保留用户数据 | `%LOCALAPPDATA%\WindowMark\settings.conf` 保留（未加 `/Purge`） |
| 卸载器自删除 | `%TEMP%` 中无残留副本 |

「已运行就报错」这一类问题在三处都已消除：安装器、卸载器、以及 `WindowMark.exe` 自身的第二次启动。

## 未验证 / 已知限制

- ~~安装器 / 卸载器的 GUI 对话框~~ — **用户人工确认无问题**（v0.3.5）。静默路径此前已完整自动验证，两者共用同一套执行逻辑。
- **hover 展开、DWM 缩略图预览、`Ctrl+Alt+W` 快捷键未自动化验证。** 这些需要真实鼠标悬停和键盘输入，截图脚本无法覆盖。请手动确认。
- **书签右键菜单（重命名 / 设置）未实测。** 需要在书签上真实右键，脚本无法投递。设置项与托盘菜单走的是同一个处理函数，那条路径已验证；重命名对话框本身未打开过。Core 侧的自定义名称逻辑有单元测试覆盖（覆盖标题、刷新后保留、清空回退、随窗口消失清理）。
- **悬停预览的层级已改**（不再以宿主窗口为跨进程 owner，改为显示时置顶），但**未实测**预览是否真的出现。
- **底部行（最大化窗口）的最终外观未截图验证。** 需要同时满足「窗口最大化」且「该窗口是前台」，脚本无法稳定构造：从后台进程调 `SetForegroundWindow` 被前台锁拒绝，模拟 Alt 解锁反而让别的应用抢走焦点。绘制代码与已验证的侧边布局共用同一段逻辑，只是根部选了另一条边，但请手动最大化一个多窗口应用确认一眼。
- **混合 DPI 未验证。** 现在是双屏，但两块屏缩放相同（都 125%），所以只验证了跨屏的坐标处理（副屏上的 Notepad4 与资源管理器边框位置正确），没有验证跨不同缩放拖动。渲染目标已固定 96 DPI 并配合 per-monitor v2 感知，理论上正确，需要一块不同缩放的屏才能确认。
- ~~书签在副屏上~~ — **用户人工确认无问题**（v0.3.5）。边框的层级审计此前已覆盖两块屏上的窗口；书签这一条是人工看的。
- **CPU 测量噪声较大。** 单次运行波动可达 ±20%，上表给出的是 5 次运行的中位数。据此判断小幅优化效果并不可靠，只有量级变化才有意义。
- **macOS 后端仍是脚手架**，未编译、未运行。
- **边框与 tacky-borders 共存未测试。** 两者会各画各的，出现双层边框属预期；请在启用本功能后停用 tacky-borders。
- **边框会被压在上面的窗口切掉，这是设计的直接后果。** 边框锚在目标窗口自己的深度上（不是浮在最顶层），所以任何盖住目标周边的窗口也会盖住那一段边框。实测任务管理器被 Claude 窗口部分遮挡时：左边 5 个采样点全是 `7080AA` 正常，右边 5 个点全是 `FCFCFB`——那是 Claude 的内容，不是边框没画。用 `check_border.bat` 可以直接区分「没画」和「被盖住」。

  想让边框永远不被切，把偏移设成 `-线宽`（例如 `width=4, offset=-4`，`Reach = max(0, 4-4) = 0`），边框就整个画在窗口内侧，只要窗口本身没被盖住就一定可见。代价是盖住窗口最外面 4px 的内容。
- **无激活的纯 z 序变化会有短暂错层。** 应用把窗口抬高但不夺焦点时收不到事件，边框要等下一个无关事件才归位。`EVENT_OBJECT_REORDER` 实测对顶级窗口不触发（静置 10 秒 0 次、抬升 6 个窗口 0 次），只剩轮询一条路，与「对系统影响极小」冲突，故不做。详见 ROADMAP 第 5 项。

  顺带澄清一个**不是** bug 的现象：Snipaste 的贴图窗口没有边框，是因为 `IsEligibleTopLevelWindow` 明确排除了「有属主的窗口」和 `WS_EX_TOOLWINDOW`，而它们两条都占。这是书签沿用至今的过滤规则，边框共用，属于有意为之。

## 版本策略

- **v0.2.0** 是第一个正式发布版本，取代从未发布的 `v0.1.0-rc1`..`rc5` 系列（rc1/rc2 只在 Linux 上构建过，rc3 起是 Windows 平台的适配过程）。它在 v0.1.0 的目标之上交付了设置界面、书签重命名、书签右键菜单和关于对话框。
- **v0.3.0**（本次）新增窗口边框：与书签完全独立的第二个功能，各有开关与设置窗口，共用同一套窗口跟踪，仍是单进程、单托盘图标。
- **v0.3.7**（当前）重排托盘菜单，加入全局暂停/启用与开机启动开关，并让设置对话框高度随字段表自动计算。

后续修复进入 `v0.3.x`，新功能进入 `v0.4.0`。
